后端构建：`cd backend && mvn clean install -DskipTests`；全量测试：`mvn verify`（含单元测试与 *IT 集成测试）；单模块测试：`mvn test -pl counseling-ai`；前端开发：各 frontend/* 目录下 `npm run dev`；一键部署：`deploy/setup-server.sh` 或 `docker compose -f docker-compose.prod.yml up -d`。