- 设计文档统一采用 NN_主题.docx 编号制（01~15 连续），新增文档必须接着最大编号往后排，保持编号连续性。
- 目录结构调整必须先修改 STRUCTURE.md 再实际移动目录，变更需 commit 中说明原因，禁止先动目录后补文档。
- Agent 规则文件归属中央库，项目内 AGENTS.md 与 .qoder/rules/ 只接受 sync-rules.sh 同步写入，禁止直接编辑项目特有内容。
- 所有临时产物（调试输出、脚本预览、下载暂存）统一放入 tmp/，tmp/* 已 gitignore，不允许在根或桌面散落临时文件。
- Prompt 历史记录采用 YYYYMMDD-用途说明.md 命名约定，生产 prompt 后续按模块命名，与探索期记录区分开。