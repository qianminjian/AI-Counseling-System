本模块是纯文档与工具层，尚未进入代码实现阶段（src/ 待启动），整体按「约束先行」原则组织为四个层次：
- 顶层导航与约定：README.md 提供目录索引，STRUCTURE.md 作为目录结构的单一事实源（新增目录必须先改此文件再动目录）。
- 设计层 `design/`：15 份编号 .docx 正式设计文档（01~15，CBT 流程、风险识别、SaaS 多学校隔离、数据库、MVP、合规等）+ BEACON.md 作为项目明灯，所有新任务启动前必读。
- Prompt 资产层 `prompts/`：记录探索期 prompt 历史，命名采用 YYYYMMDD-用途说明.md。
- 工具脚本层 `scripts/`：7 个 Node.js/Python 一次性脚本，用于把设计要点再生成回 design/ 下的 .docx 编号文档，默认输出到 tmp/ 预览。
- Agent 规则同步区：AGENTS.md + `.qoder/rules/` 由 sync-rules.sh 从本地中央库 `/Users/minjianq/Documents/.LIB_ALL_PUB/Qoder/` 拉取，支持 --force 覆盖与 md5 一致性校验；该区域被 STRUCTURE.md §3 标记为「禁止写入项目特有内容」，形成「中央库 → 项目副本」单向依赖方向。
- 测试与源码目录 tests/、src/ 仅占位，遵循 tests/unit|integration|e2e 三层镜像 src/ 结构约定。