---
kind: error_handling
name: 错误处理体系（尚未落地）
category: error_handling
scope:
    - '**'
source_files:
    - AGENTS.md
    - STRUCTURE.md
    - scripts/create_agent_workflow_doc.js
---

本仓库目前处于设计文档阶段，`src/` 目录为空骨架（仅 `.gitkeep`），尚未引入任何编程语言或框架，因此不存在实际的错误处理实现代码。

在现有资产中可观察到的相关线索：
- `scripts/create_agent_workflow_doc.js` 等脚本使用 Node.js 的 `.catch(err => console.error(...))` 进行最基础的 Promise 错误捕获，属于一次性工具脚本的简单实践，不构成项目级规范。
- `AGENTS.md` §7.2 提到规则文件 `code-engineering.md` 包含「错误处理」条目，但 `.qoder/rules/` 目录当前为空（由中央库同步区管理，本地未同步到该工作区），无法确认具体约定内容。
- `STRUCTURE.md` 与 `README.md` 均未定义错误码、错误类型、中间件或 panic/recover 策略。
- 设计文档（`design/`）聚焦产品架构、CBT 流程、Prompt 体系与安全合规，未发现技术栈选型后的错误处理专项设计。

结论：该项目尚未进入编码阶段，错误处理体系尚未落地。待技术栈确定后，应在 `src/` 下建立统一的错误类型定义、传播与展示机制，并参考 `AGENTS.md` 引用的 `code-engineering.md` 中的「错误处理」规则执行。