# 学生端语音引擎 Browser Agent 遍历测试报告（2026-08-10 第二轮）

> 专题：design/15 语音引擎专项（C 系列 + S 系列语音相关）| 执行日期：2026-08-10
> 环境：生产 UAT（https://yun.gxjugu.com/mindsafe/）+ chrome-devtools MCP
> 验证版本：F-19~F-27 全量修复后（预启动 Worker / 并发下载 / 时间戳日志 / 近音匹配）
> 配套：问题登记见本报告 §三（发现与观察项表，2026-08-13 台账修正：原引用 `ISSUES-语音引擎.md` 不存在，本专题问题已内置于报告）

---

## 一、场景执行结果

| 场景 | 结果 | 关键证据 |
|------|------|---------|
| **S-预加载**（登录页并发预加载） | ✅ PASS | 0.27~0.79s 声纹 + Worker 并发启动，Worker 独立下载（F-26） |
| **C-04 引擎就绪（缓存命中）** | ✅ PASS | 声纹 184ms + Worker 412ms，**全链路 0.72s ready**（刷新后秒就绪） |
| **C-04 引擎就绪（从零下载）** | ✅ PASS | **Worker 独立下载 105s（3Mbps）→ session_create 3.7s → 109s ready**，全程无卡死无降级 |
| **S-04 进对话时序** | ✅ PASS | 49.4s 进对话 → Worker 复用（resolve 零等待）→ 51.9s standby（2.5s 延迟提示） |
| **C-04 唤醒触发（F-20 近音实证）** | ✅ PASS | 转写 id=3 **"哈喽,伯伯"** → 近音匹配命中（伯/波/啵/播/铂+伴/宝）→ UI 进入"我在听" |
| **C-01 转写循环** | ✅ PASS | 转写 id=1~5 持续运行（"好"/"哈喽"/"哈喽伯伯"/"多多"等环境音识别） |
| **C-07 冷却关窗** | ✅ PASS | active 态超时（25s 无说话）→ 自动回 standby（状态机正常） |
| **S-04 对话/TTS** | ✅ PASS | 文字消息发送 → AI 回复（TTS 欢迎语已播实证） |

## 二、时间戳轨迹（从零下载全流程，本次实证）

```
0.79s    preloadVoiceprintModel + preloadWakeModel 并发启动
0.80s    Worker 创建 + init 已发送（独立下载开始，F-26 并发模型）
81.03s   声纹模型加载完成（6.7MB 下载 + 初始化）
109.15s  Worker ORT session_create 完成（108.7s：下载 ~105s + 初始化 ~3.7s）
110.03s  Worker ready（预启动就绪 109s）
~115s    登录测试丁 → 情绪 → 开始聊天
+5s      开启语音唤醒 → standby（Worker 复用零等待 + 2.5s 延迟提示）
+8s      转写循环运行（transcribe id=1 提交）
```

**缓存命中场景**（对照）：0.27s 预加载 → 声纹 184ms + Worker 412ms → 0.72s ready。

## 三、发现与观察项

| # | 类型 | 内容 | 结论 |
|---|------|------|------|
| 1 | 观察 | **首次下载耗时 ~110s**（唤醒 43MB + 声纹 6.7MB @ ~3Mbps 带宽） | 物理带宽限制，非 bug；登录页停留期完成，进对话即就绪 |
| 2 | 观察 | 11:39 一轮首次下载在 17.44s 停滞（console 无更新，网络层完成） | 网络瞬态（本次复现正常，109s 走通）；Worker init 超时 180s 已覆盖，无需改代码 |
| 3 | 建议 | **progress 日志刷屏**：首次下载 3000+ 条 console.info（"Worker 消息: progress"） | 诊断价值低 + 大流量场景可能拖慢 DevTools 会话；建议生产降级为 debug 或 5% 采样 |
| 4 | 通过 | 唤醒词近音匹配（F-20）端到端实证："哈喽伯伯"命中 | 无需修改 |

## 四、结论

**学生端语音引擎全链路（从零下载 → 就绪 → 唤醒 → 转写 → 冷却）通过**：
- 首次下载路径完整走通（109s，带宽限制内）
- 缓存命中秒级就绪（0.72s）
- 唤醒触发实证（F-20 近音匹配）
- 转写循环 + 冷却状态机正常

**待办**：观察项 3（progress 日志降频）建议后续优化；C-04 音频变体/抗误唤醒端到端仍待真机（假麦克风受限）。
