# 家长端 H5 与小程序设计

> 版本：v1.0 | 创建日期：2026-07-26
> 对齐：design/25 §五（家长端功能）、design/24 §5.3（家长 Token 绑定手机）、design/16 §2（家长 API）

---

## 1. 产品定位

家长端是 MindSafe "学生-AI-教师-家长"四位一体体系的最后一环。家长通过教师分享的链接，在手机端查看孩子的情绪周报、接收 AI 建议、管理数据授权。

**核心原则：**
- 只读为主：家长只看统计报告，不看对话原文（隐私保护）
- 零注册：无需下载 App / 注册账号，链接即入口
- 合规闭环：监护人同意确认、同意撤回均可在家长端完成

---

## 2. 分阶段规划

| 阶段 | 形态 | 时间 | 核心内容 |
|------|------|------|---------|
| **P1（本期）** | H5 移动网页 | 当前迭代 | Taro 4 编译为 H5，nginx 静态部署，链接访问 |
| **P2（远期）** | 微信小程序 | 后续规划 | 同一 Taro 工程编译为 weapp，补微信 OAuth，提审上线 |

### 2.1 P1 → P2 复用分析

| 层 | 复用度 | 迁移工作 |
|---|---|---|
| API 调用层（`src/api/`） | 100% | Taro `request` 统一封装，H5/小程序通用 |
| 业务逻辑（hooks/utils） | 95% | 纯 JS，无平台依赖 |
| 视图层（页面/组件） | 85% | Taro 组件跨端兼容；少量 CSS 适配 |
| 认证流程 | **需改造** | H5 用 JWT Token；小程序改 `wx.login` → openid → 绑定 |
| 路由/生命周期 | 自动适配 | Taro 框架处理 |

### 2.2 P2 小程序阶段待办（远期，不丢失）

| 任务 | 说明 | 依赖 |
|------|------|------|
| PARENT-WX-001 | 微信小程序工程注册 + AppID 配置 | 企业主体认证 |
| PARENT-WX-002 | wx.login → openid → parent_bindings 绑定 | 后端 parent_bindings 表（已设计） |
| PARENT-WX-003 | 微信 OAuth 授权页（获取手机号） | 微信开放平台配置 |
| PARENT-WX-004 | `taro build --type weapp` 编译 + 真机调试 | Taro 工程已就绪 |
| PARENT-WX-005 | 小程序提审 + 上线 | 隐私协议 / 类目审核 |
| PARENT-WX-006 | 订阅消息推送（周报通知） | 微信订阅消息 API |

---

## 3. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 框架 | **Vite 6 + React 19** | 轻量、构建快、与学生端/教师端技术栈统一 |
| 路由 | **react-router-dom 7** | SPA 路由，3 个页面足够 |
| 样式 | **SCSS**（单文件 app.scss） | 移动端适配，max-width 480px 居中 |
| 部署 | nginx 静态目录 `/parent/` | 复用现有 nginx，路径 `yun.gxjugu.com/parent/` |
| 状态管理 | React useState/useEffect | KISS，页面少无需 Redux |

### 3.1 P2 小程序迁移方案

P1 代码结构已按跨端兼容设计：
- API 层（`src/api/`）：纯函数，P2 将 `fetch` 替换为 `Taro.request`
- 业务逻辑（`src/utils/`）：纯 JS，无平台依赖
- 视图层：P2 用 Taro CLI 初始化工程，将 HTML 元素替换为 Taro 组件

迁移工作量估计：2-3 人天（主要是视图层改写 + 微信 OAuth 接入）

---

## 4. 页面设计（P1 H5）

### 4.1 页面清单

| 路由 | 页面 | 功能 |
|------|------|------|
| `/` | 手机验证页 | 家长打开链接 → 输入手机号 → 获取验证码 → 验证通过 → 跳转周报 |
| `/report` | 情绪周报页 | 展示近 7 天情绪分布、对话统计、风险等级、AI 建议 |
| `/consent` | 同意管理页 | 查看授权状态 + 撤回同意（不可逆操作，二次确认） |

### 4.2 手机验证页（`/`）

```
┌─────────────────────────────┐
│  MindSafe 家长端             │
│                             │
│  请输入您的手机号验证身份     │
│                             │
│  手机号  [138 1234 5678]    │
│  验证码  [______] [获取]    │
│                             │
│  [验证并查看报告]            │
│                             │
│  ─────────────────────────  │
│  验证通过后即可查看孩子      │
│  近 7 天的情绪周报           │
└─────────────────────────────┘
```

**流程：**
1. 链接含初始 Token（教师生成，`?token=xxx`）
2. 家长输入手机号 → `POST /parent/send-code`
3. 输入验证码 → `POST /parent/verify-phone` → 获得正式 7 天 Token
4. Token 存 localStorage → 跳转 `/report`

### 4.3 情绪周报页（`/report`）

```
┌─────────────────────────────┐
│  🌈 小明的情绪周报           │
│  2026-07-20 ~ 2026-07-26    │
│                             │
│  ┌─ 情绪分布 ──────────┐    │
│  │ 😊 开心  45%  ████  │    │
│  │ 😐 平静  30%  ███   │    │
│  │ 😢 难过  15%  ██    │    │
│  │ 😰 焦虑  10%  █     │    │
│  └─────────────────────┘    │
│                             │
│  本周对话：5 次 / 共 42 轮   │
│  风险状态：🟢 良好           │
│                             │
│  ┌─ AI 建议 ──────────┐     │
│  │ 孩子本周整体情绪稳  │     │
│  │ 定，建议周末安排户  │     │
│  │ 外活动...           │     │
│  └─────────────────────┘    │
│                             │
│  [同意管理]                  │
└─────────────────────────────┘
```

**数据来源：** `GET /parent/report`（Header: Bearer token）

### 4.4 同意管理页（`/consent`）

```
┌─────────────────────────────┐
│  数据授权管理                │
│                             │
│  当前状态：✅ 已授权         │
│  授权时间：2026-07-01       │
│  授权版本：v1.0             │
│                             │
│  ─────────────────────────  │
│  ⚠️ 撤回同意将：            │
│  · 冻结孩子的账号           │
│  · 删除心理画像数据         │
│  · 此操作不可逆             │
│                             │
│  [撤回同意]（红色按钮）      │
│                             │
│  如需恢复，请联系学校重新    │
│  生成授权链接。              │
└─────────────────────────────┘
```

**操作：** `POST /parent/consent/withdraw`（二次确认弹窗）

---

## 5. 工程结构

```
frontend/parent-h5/
├── src/
│   ├── api/
│   │   └── index.js          # API 封装（fetch，P2 换 Taro.request）
│   ├── pages/
│   │   ├── verify/
│   │   │   └── index.jsx     # 手机验证页
│   │   ├── report/
│   │   │   └── index.jsx     # 情绪周报页
│   │   └── consent/
│   │       └── index.jsx     # 同意管理页
│   ├── utils/
│   │   └── auth.js           # Token 管理（localStorage）
│   ├── main.jsx              # 入口（React Router）
│   └── app.scss              # 全局样式
├── index.html                # Vite 入口 HTML
├── vite.config.js            # Vite 配置（base: /parent/）
└── package.json
```

---

## 6. 后端 API（已实现）

| 端点 | 方法 | 功能 | 状态 |
|------|------|------|------|
| `/api/v1/parent/send-code` | POST | 发送手机验证码 | ✅ |
| `/api/v1/parent/verify-phone` | POST | 验证手机 + 签发 Token | ✅ |
| `/api/v1/parent/report` | GET | 情绪周报数据 | ✅ |
| `/api/v1/parent/consent/withdraw` | POST | 撤回同意 | ✅ |

无需新增后端代码。

---

## 7. 部署方案（P1 H5）

```nginx
# nginx 新增 location
location /parent/ {
    alias /opt/mindsafe/frontend/parent-h5/dist/;
    try_files $uri $uri/ /parent/index.html;
}
```

构建命令：`cd frontend/parent-h5 && npx taro build --type h5`

---

## 8. 安全与合规

| 项 | 措施 |
|---|---|
| Token 安全 | 初始 Token 仅用于验证流程；正式 Token 7 天过期 |
| 手机号脱敏 | 前端展示 `138****5678`，后端日志同 |
| 只读原则 | 家长端无写入接口（除撤回同意），不暴露对话原文 |
| 撤回不可逆 | 二次确认弹窗 + 明确文案告知后果 |
| PIPL 告知 | 验证页底部链接到告知同意条款（design/22） |

---

_本文档由 Agent 维护，代码实现与文档同步更新。_
