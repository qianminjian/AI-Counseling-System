# frozen/91 平台表 schema 边界独立专题（Q-003/Q-007 统一归口）

> 创建：2026-08-11 | 来源：doing/91 第四轮架构深化候选清单 Q-003（平台级表物理落 tenant_template schema）+ Q-007（迁移序列一致性）
> 背景：**项目负责人按建议执行（2026-08-11）：schema 边界涉生产迁移，冻结独立专题跟踪**——
> 根因 = 15+ 平台表（degradation_events/alert_events/platform_admin/device*/toc_*/usage 等）物理落
> tenant_template schema（V34 注释自称"平台级"），schema 命名承载"模板"语义 → 边界漂移 +
> 租户创建若复制模板 schema 则平台表被误复制。
> 关联：doing/91（来源专题，Q-003/Q-007 ❄️ 冻结移交）；doing/92 R-023（schema 快照门禁已落地）；
> frozen/90（声纹向量化，V46 平台表同源问题）；Q-002 一致性测试（名单已兜底，短期无故障面）。

## 一、跟踪项

| 归口项 | 来源 | 现状 | 解冻条件 |
|---|---|---|---|
| **Q-003 平台表 schema 边界**（tenant_template → 独立 platform schema 或显式重命名语义） | doing/91 Q-003 | V34/V35/V39/V42-45 平台表挂 tenant_template；风险为潜在（需核实租户创建流程是否复制整个 schema） | 独立批次立项（迁移搬迁 + 回滚设计 + 引用更新）；或核实租户创建仅复制部分表后降级关闭 |
| **Q-007 迁移序列一致性**（模式收敛） | doing/91 Q-007 | 新增迁移延续平台表挂 tenant_template 模式 | 连带 Q-003（Q-002 一致性测试已兜底名单） |

## 二、根因说明（为什么不能顺手完成）

- **生产迁移红线**：15+ 表搬迁至新 schema 涉及引用更新（外键/视图/代码 schema 硬编码 37 实体）+ 回滚设计
- **风险为潜在**：当前无故障证据（Q-002 名单兜底 fail-fast）；实际故障需"租户创建复制整个模板 schema"成立
- 需独立 SPEC（搬迁清单 + 回滚 + 验证），非小步可交付

## 三、解冻流程

以下任一条件成立时由项目负责人发起解冻：
1. 核实租户创建流程确实复制整个 tenant_template schema（风险升级为实际）
2. 平台表数量继续增长（Q-002 测试名单持续膨胀）
3. 信创/多租户扩容批次立项（frozen/41 关联）

## 四、关联跟踪

- **Q-002 一致性测试**（已实施）：平台表必入 IGNORE_TABLES——schema 边界修复前名单是唯一兜底，勿删
- **R-023 schema 快照门禁**（已实施）：01_schema.sql 生成含 schema 归属——搬迁后自动反映
- **37 实体 @TableName schema 硬编码**（doing/92 R-020）：随 Q-003 批次收敛为公共常量
