# frozen/88 企微告警推送对接（OPS-MON-005 演练依赖）

> 状态：冻结（远期任务，触发条件未到不实施、不合并） | 编号：88（接续 frozen/87）
> 创建：2026-08-10 | 来源：doing/86 部署验证项 2（OPS-MON-005 降级演练）+ OPS-MON-004 告警触达
> 关联：doing/86（部署验证专题）；design/14 §M2/M3（告警中心/降级监控）；frozen/87（LLM 成本跟踪，独立）

---

## 一、冻结内容

**企微应用消息告警推送对接**——AlertManager 企微 receiver 的正式配置与验收：

| 项 | 说明 |
|----|------|
| 所需密钥 | `WECOM_CORP_ID` / `WECOM_AGENT_ID` / `WECOM_SECRET`（+ 可选 `WECOM_ALERT_TO_USER`） |
| 当前状态 | **哨兵值部署**（`not-configured`/`0`）——监控栈已运行，告警记录与页面可见，仅不推送企微 |
| 验收标准 | 断 DASHSCOPE key → tts 降级 → 企微 ≤5min 收到 `TtsPrimaryEngineDegraded`（doing/86 项 2，AC-6） |
| 解锁条件 | 企业微信应用凭证就绪（管理员提供 CorpID/AgentID/Secret） |

## 二、实施步骤（解冻后执行）

1. 企业微信创建自建应用 → 获取 CorpID/AgentID/Secret
2. `/guju/mindsafe/.env` 配置四项（红线：密钥不入 git）
3. 重启 mindsafe-alertmanager 容器（compose 环境变量生效）
4. 触发降级演练（doing/86 项 2）→ 验证企微触达 ≤5min
5. 演练通过 → doing/86 项 2 闭环 + 本专题归档

## 三、冻结期间的可用能力（不阻塞）

- Prometheus 指标采集 ✅（不依赖 WECOM）
- AlertManager 告警记录 + admin-web 告警中心展示 ✅（推送失败仅日志）
- Grafana 看板 + 指标查询 ✅
- 告警触达仅缺失（页面/台账可见性不受影响）
