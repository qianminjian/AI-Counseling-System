# doing/90 轻量安全扫描报告（全项目静态分析）

> 编号：90（doing 区接续；与 doing/90 第三轮架构深化同号异题——develop 线已归档 his/90，main 线复用编号不冲突）
> 创建：2026-08-11 | 扫描方式：静态代码审计（轻量 19 项扫描：硬编码密钥/SQL 注入/命令执行/XSS/CSRF/文件上传/跨域/信息泄露/敏感文件等）
> 扫描范围：全项目后端 Java + Python + 前端 TSX/TS + 配置 + 部署脚本
> 结论：**通过**——P0/P1 级安全风险 0 项
> 背景：AUDIT-DEEP 审计修复（设备 token 过期 signed/P1-01、serverUrl 加固 P1-02）与 AC-89-04/05（认证接缝统一）等核心安全修复已全部落地后，轻量全量复检。

---

## 一、扫描结果总表

| 检查项 | 结果 | 证据 |
|--------|------|------|
| 1. 硬编码密钥/密码/Token | 🟢 无 | 全部 `${VAR}` 环境变量引用，yml 零明文 |
| 2. SQL 注入风险（MyBatis `$` 拼接） | 🟢 无 | 零 `$` 拼接；`.last("LIMIT 20")` 为固定字面量（N-009 已标注接缝） |
| 3. 命令/脚本执行（Python） | 🟢 安全 | 仅 `subprocess.run` ffmpeg（`shell=False` 默认，已设超时） |
| 4. XSS/危险 HTML | 🟢 无 | `dangerouslySetInnerHTML`/`innerHTML`/`document.write` 零使用 |
| 5. 硬编码密钥/SECRET_KEY | 🟢 无 | 仅注释和路径常量（`RESOURCE_PATH`/`keywords` 非密钥） |
| 6. 过宽跨域（`Access-Control-Allow-Origin: *`） | 🟢 无 | 后端 CORS 配置无通配符 |
| 7. 系统信息泄露（`printStackTrace`/`System.out`） | 🟢 无 | 量代码零残留 |
| 8. Redis/DB 密码配置 | 🟢 无 | yml 零明文（`REDIS_PASSWORD: ${REDIS_PASSWORD}`） |
| 9. JWT 密钥硬编码 | 🟢 无 | 配置中零明文（环境变量注入） |
| 10. Python `shell=True` | 🟢 无 | `subprocess.run` 默认 `shell=False` |
| 11. 前端 token 暴露 | 🟢 安全 | admin-web 已迁 `sessionStorage`（P0 完成）；家长端 toC 前缀 |
| 12. 文件上传验证 | 🟢 安全 | voice-service 后缀白名单 + 10MB 限制 + ffmpeg 超时 |
| 13. API 无认证检查（permitAll） | 🟢 合理 | 11 个公开端点均有业务必要性（登录/声纹/TTS/配置/device）+ 独立防护（限流/DVC token） |
| 14. Python `print()` 调试泄露 | 🟢 无 | 零残留 |
| 15. CSRF 防护 | 🟢 合理 | API `csrf.disable()`（Bearer token 认证，CSRF 不适用） |
| 16. 备份/示例文件泄露 | 🟢 无 | `.env` 不在 git 跟踪；`.env.example` 正常 |
| 17. 依赖已知漏洞（Log4j/Jackson/SnakeYAML） | 🟢 Spring Boot BOM 管控 | `pom.xml` 版本由 `spring-boot-starter-parent` 管理，无独立低版本声明 |
| 18. 敏感路径/文件 | 🟢 无 | `backend/.env` 未跟踪、`deploy/.env.example` 正常 |
| 19. 安全头部（nginx） | 🟡 建议 | `deploy/nginx.conf` 未配置 `X-Content-Type-Options`/`X-Frame-Options`/`Strict-Transport-Security`；生产 nginx 配置在宿主机独立管理 |

---

## 二、核心安全防线（已落地验证清单）

| 防线 | 文件/位置 | 完成时间 |
|------|----------|---------|
| 设备 token 4 段化（HMAC+24h 过期+常量时间比较） | `DeviceSecurityService.java` | AUDIT-DEEP-001 |
| reportOnline 已存在设备需 token 回连 | `DeviceService.java` L100 | AUDIT-DEEP-002 |
| pullConfig 绑定设备 token 校验 | `DeviceService.java` L316 | AUDIT-DEEP-002 |
| 公开端点 IP 限流（voiceprint/verify/device） | `RateLimitInterceptor.java` | AUDIT-DEEP-011 |
| 家长端 login 锁定 5 次/15 分钟 | `ParentAuthService.java` | AC-89-01 |
| withdrawn 同意撤回统一拦截（ParentIdentityResolver） | `ParentIdentityResolver.java` | AC-89-04 |
| AuthProvider 统一签发（四体系） | `AuthProvider.java` + 4 实现 | AC-89-05 |
| 危险操作 X-Confirm 统一校验（5 处收编） | `OpsController.java` requireConfirm() | N-008 |
| 覆盖键运行时消费（tts/voice fail-open） | `tts_policy.py` / `voice-service/app.py` | RUNTIME-001/002 |

---

## 三、建议改进项（P3 级，非紧急）

| 项目 | 说明 | 建议 | 工作量 |
|------|------|------|--------|
| nginx 安全头部 | `deploy/nginx/host/nginx.conf` yun 块补 `X-Content-Type-Options`/`X-Frame-Options`/`Strict-Transport-Security` | ✅ 已实施（2026-08-11）；`sync_host_nginx` 部署时自动同步宿主（nginx -t 门禁 + reload） | ~5 行 |
| Spring Security 安全头部 | `SecurityConfig` 补 `.headers()`（frameOptions DENY + contentTypeOptions） | ✅ 已实施（2026-08-11） | ~3 行 |
| permitAll 端点定期审计 | 11 个公开端点列表需持续维护 | ✅ 已实施（`SecurityConfig.java` 注释块 + 各端点独立防护措施标注） | 注释 |
| 前端 token 存储统一审查 | 三前端（student-h5/parent-h5/teacher-web）token 存储方式 | ✅ 审查通过（2026-08-11）：student-h5 `api.ts` token/user 已存 **sessionStorage**（关闭 tab 自动清除）；consent 同意标记/声纹录入标记/UI 位置偏好存 localStorage（跨会话持久，业务合理）；parent-h5/teacher-web 同模式。**无代码改动** | 审查完成 |
| CI 依赖漏洞扫描 | 后端/前端依赖已知漏洞（Log4j/Jackson/SnakeYAML 等） | ✅ 已存在（ci.yml L292/304）：Trivy backend + frontend 双 job，CRITICAL/HIGH 阻断合入（exit-code 1），无需新增 | — |
| 生产 nginx 同步 | host nginx.conf 安全头部是否同步到生产 | ✅ 机制已存在（DA-13）：`deploy/nginx/host/` 唯一事实源 → deploy.sh `sync_host_nginx` 每次部署上传宿主 + `nginx -t` 门禁 + reload；安全头部随下次部署生效 | 部署时生效 |

---

## 四、与之前审计问题的对照

| 之前审计问题（AUDIT-DEEP/doing/88/89） | 当前状态 |
|---------------------------------------|---------|
| P1-01 设备 token 无过期校验+自签名 | ✅ 已修复（4 段化 HMAC+exp） |
| P1-02 reportOnline serverUrl 劫持链 | ✅ 已修复（token 回连 + 仅首次设置） |
| P3-04 公开端点无限流 | ✅ 已修复（IP 限流 300/min） |
| AC-89-03 withdrawn 旁路 | ✅ 已修复（ParentIdentityResolver 统一拦截） |
| AD-006 声纹 read-modify-write 非原子 | ✅ 已修复（Lua CAS） |
| N-008 requireConfirm 5 处拷贝 | ✅ 已修复（统一收编） |
| N-009 画像域注释误导 | ✅ 已修正 |

---

**结论**：全项目安全卫生状况良好。核心安全修复已全部落地且通过回归验证（后端 1096+454、前端全量）。建议改进项均为 P3 级别（安全头部加固），威胁模型无新增缺口。