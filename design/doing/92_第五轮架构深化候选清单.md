# doing/92 第五轮架构深化候选清单（R-001~R-029）

> 编号：DOC-115 | 创建：2026-08-11 | 来源：improve-codebase-architecture 第五轮（**已排除** his/88 AD + his/89 N + his/90 P + doing/91 Q——用户指令直接生成 md 到 doing）
> 状态：doing（候选清单，待逐项议决实施）| doing 区接续编号（87/88/89/91/92）
> 关联：doing/91（Q-001~010 待议决）、his/90（已归档）、frozen/90（声纹向量化）
> 架构词汇：模块/接口/深度/接缝/适配器/杠杆/局部性；删除测试结论逐项标注

---

## 一、背景与走查结论

第五轮排除前四轮已审计范围，聚焦前端深部（student-h5/teacher-web/parent-h5 服务面）/ 后端未审服务域（consent/safety/billing/usage/relaxation/diary）/ 认证深部（JwtTokenProvider/Filter）/ domain 层 / 测试数据部署面：

- **Strong 4**：R-001 student-h5 API 无端点登记 / R-007 consent 撤回后无法重新授权（合规敏感）/ R-010 业务日界依赖服务器时区 / R-016 JwtTokenProvider tokenType 字面量 + 前缀剥离 4 处重复（近三天修复暴露根因）
- **Worth 15** / **Speculative 8** / **删除测试 10**（含 ChartCard/StatCard 已删除证实、01_schema.sql 过期 16 版、DEMO2026 演示后门等）

**近三天新增摩擦 3 项**：JwtTokenProvider 前缀剥离（R-016，4a5cd516 修复暴露根因）/ JwtAuthenticationFilter 6 次 parse（R-017）/ consent 触碰面（R-007/008）。

## 二、候选清单

### 前端深部

#### R-001 student-h5 API 服务面无端点登记【Strong】
- **涉及**：student-h5/src/api.ts（400 行四类职责：存储键/会话/9 个硬编码 fetch/通用 api()）
- **问题**：三端端点登记模式不一致——teacher-web 有 ENDPOINTS 常量表（FA-15）、parent-h5 有 endpoints.ts，student-h5 无等价物；新增端点靠手写路径 + apiContract 正则测试兜底（开发期无编译期提示）。
- **解决**：9 个 fetch + api() 收拢为 student 端 ENDPOINTS 常量表 + 统一入口（对齐 teacher-web 模式）；ConsentKeys 迁出独立模块。
- **删除测试**：getUser() 返回 Record<string, unknown>——类型弱但调用面可用，不强求。

#### R-002 teacher-web 审计日志端点资源归属错位【Worth】
- **涉及**：teacher-web api/endpoints.ts L51 `getAuditLogs: '/api/v1/admin/invite-codes/audit-logs'`
- **问题**：审计日志（全局资源）挂在 invite-codes 子资源下，REST 语义错位。
- **解决**：迁移至 /api/v1/admin/audit-logs（配向后兼容别名或一次性迁移）。

#### R-003 parent-h5 请求工厂三处自建 + toc 绕过 TokenStorage【Worth】
- **涉及**：parent-h5 services/index.ts + device.ts + toc.ts（三处 createPlatformRequest 独立实例）；toc.ts saveTocSession 直写 sessionStorageImpl（键名靠"巧合一致"）
- **问题**：三工厂实例分裂；toc 会话落盘绕过 TokenStorage 接口（无契约）；tokenStorage.ts L8 注释声称的"parent 适配层包装"**实测不存在**（注释应删或适配应补）。
- **解决**：服务层共享单一工厂实例（模块级单例）；toc 落盘改走 TokenStorage 接口。

#### R-004 useChatSession 编排深度 + deduplicateText 无单测【Worth】
- **涉及**：student-h5/src/hooks/useChatSession.ts（sendMessage ~110 行编排）+ deduplicateText（O(n²) 启发式无单测）
- **问题**：sendMessage 编排 9+ hooks 协调（ref 代理规避 TDZ——继续增长逼近"接线意大利面"）；deduplicateText 业务规则裸露。
- **解决**：deduplicateText 抽纯函数 + 单测（对比 chatRoomRules.ts 有配套测试）；sendMessage 可拆"发送编排/流收尾"两层。

#### R-005 teacher-web 色板双轨（StatsCharts MS 硬编码 vs CSS token）【Worth】
- **涉及**：teacher-web StatsCharts.tsx L29-36（MS 色板 TS 常量，注释自述与 index.css token 手工同步）；useECharts.ts 双 init 路径重复
- **问题**：主题 token 双轨（CSS var 与 TS 常量手工同步）；useECharts 双 init 靠 chartRef 防重。
- **解决**：色板从 shared 主题模块导出（或生成 CSS+TS 双产物）；useECharts 双 init 收敛单 effect + 重试。
- **删除测试**：ChartCard/StatCard **已删除**（被 StatsCharts/useECharts 取代，无残留——用户点名走查项证实健康）。

#### R-006 teacher-web DTO 手工对齐后端 VO【Worth】
- **涉及**：teacher-web src/api.ts L83-201（20+ DTO 手工镜像）+ getStudentRadar 返回 Record<string, any>
- **问题**：纯手工镜像无生成器，类型级漂移风险持续。
- **解决**：接入 openapi-snapshot 管线生成 DTO（student-h5 用法成熟可复用）。

### 后端服务域

#### R-007 consent 撤回后无法重新授权【Strong（合规敏感）】
- **涉及**：ConsentWithdrawalService（L91-93 撤回只 insert 留痕不删 guardian_consent）+ GuardianConsentService.requestConsent（L68-76 selectCount > 0 拒绝重复申请）
- **问题**：撤回后 guardian_consent 记录仍在 → **家长撤回后学生永远无法重新获得监护人同意**；账号 status=withdrawn 后无 reactivate 路径。
- **解决**：撤回语义补充"重新授权"流程（删除/失效旧 guardian_consent 或按撤回时间戳过滤）；明确 reactivate 产品路径。
- **删除测试**：ConsentWithdrawalService.STATUS_WITHDRAWN 兼容常量可删（User.STATUS_WITHDRAWN 已存在）。

#### R-008 GuardianConsentService 注释偏差 + 脱敏无长度守卫【Worth】
- **涉及**：GuardianConsentService L24-26（注释称"验证通过→激活"但 confirmConsent 只 insert 未更新 user.status）+ L80-81 手机号脱敏 substring 无长度守卫（<7 抛 StringIndexOutOfBounds）
- **解决**：注释更正（或补激活逻辑）；脱敏抽公共工具 + 长度守卫。

#### R-009 撤回数据删除覆盖面窄【Worth】
- **涉及**：ConsentWithdrawalService（撤回只删 student_profiles + 冻结账号；声纹/记忆/日记/对话均不随撤回删除）
- **问题**：PIPL §47 删除权语义下"画像已删、记忆仍在服务期"窗口。
- **解决**：定义"撤回删除范围"清单（表级）或撤回事件登记进 retention 优先队列。

#### R-010 业务日界依赖服务器时区【Strong】
- **涉及**：RelaxationService L38 ZoneId.systemDefault() / EmotionDiaryService L32/L62/L68 LocalDate.now() / UsageTimeLimitService L88 LocalDate.now()——而 service/common/CounselingTimeZone.java 已存在**无人引用**
- **问题**：三处"今日"边界随服务器时区漂移——部署非东八区时今日计数/打卡/时长限制全部错位。
- **解决**：统一走 CounselingTimeZone（三服务收敛）。

#### R-011 EmotionDiaryService 打卡非原子 + 跨域静态耦合【Worth】
- **涉及**：EmotionDiaryService checkin（L31-54 selectOne+insert 非原子 upsert，并发双击双记录）+ getStreak 直接调 BadgeService.computeStreak 静态
- **解决**：(tenant_id, student_user_id, diary_date) 唯一索引 + ON CONFLICT；streak 注入化。

#### R-012 SosEventService 去重非原子【Speculative】
- **涉及**：safety/SosEventService L58-66（selectCount 窗口 + insert 两步，并发双落）
- **解决**：(tenant_id, student_user_id, source_type, detected_at) 唯一约束或 ON CONFLICT DO NOTHING。风险低（单按钮 fire-and-forget）。

#### R-013 billing 两档模型 + 每请求查库【Speculative】
- **涉及**：TenantPlanResolver（L42-60 每次 selectById 无缓存；STANDARD/TRIAL 两档）
- **解决**：短 TTL 本地缓存；权益档位显式化（plan 字段或配置表）。

#### R-014 UsageTimeLimitService fail-open 无观测【Speculative】
- **涉及**：usage/UsageTimeLimitService（L51-63 Redis 异常全吞，无计数无告警——对比 TenantPlanResolver 有 failopen 计数）
- **解决**：补 fail-open 计数指标（对齐 AUD-014 模式）。

#### R-015 OutputSafetyReporterImpl 留痕不完整 + 记忆替换非原子【Worth】
- **涉及**：safety/OutputSafetyReporterImpl（reviewJson 参数 L96/L125 从未持久化——TC260 抽检依据不完整）；replaceLastAssistantMessage（L178-195 整表 find+saveAll read-modify-write 非原子）
- **解决**：reviewJson 落 RiskEvent 扩展字段；替换改"追加更正消息"（幂等无并发覆盖）。

### 认证/安全域

#### R-016 JwtTokenProvider tokenType 字面量 + 前缀剥离 4 处重复【Strong（近三天新增）】
- **涉及**：JwtTokenProvider（5 种 tokenType 字符串字面量 L82-212；PLATFORM_ 前缀剥离在 isPlatformToken/getPlatformRole/getPlatformAdminId/getTokenId 重复 4 处）
- **问题**：8/10 刚修 getTokenId 漏剥前缀 bug（4a5cd516"平台域 API 全部 500"）——根因正是剥离逻辑未单点化；validateToken 不校验 tokenType（"签名有效即 valid"语义陷阱）。
- **解决**：tokenType 枚举 + stripPlatformPrefix 单点助手 + 单次 parse 返回 claims 快照。
- **删除测试**：validateToken 裸用场景应删除或显式化。

#### R-017 JwtAuthenticationFilter 单请求最多 6 次 JWT parse【Worth（近三天新增）】
- **涉及**：JwtAuthenticationFilter 业务分支 L51-57（validateToken→isAccessToken→getTokenId→getUserId→getUserType→getTenantId 六次 parse）
- **解决**：filter 内单次 parse 取 claims 复用（或 TokenProvider 提供 parseOnce 快照）。

#### R-018 租户上下文双通道【Speculative】
- **涉及**：JwtAuthenticationFilter（TenantContext 挂 Authentication.details + TenantContextHolder ThreadLocal 双通道）
- **解决**：统一单一读口（details 仅审计留存）；finally 清理已防护到位。

### domain 层

#### R-019 User 工厂方法状态字面量【Worth】
- **涉及**：User.java createStudent/createTeacher（L91/L101 仍写 "active" 字面量，常量已定义 L15-40）
- **解决**：全库工厂方法统一引用常量 + 校验脚本。

#### R-020 entity schema 硬编码 + IGNORE_TABLES 手工膨胀【Worth】
- **涉及**：37 个实体 @TableName schema="tenant_template" 硬编码；MindSafeTenantLineHandler.IGNORE_TABLES 已达 22 张（doing/91 Q-002 关联）
- **解决**：schema 抽公共常量；IGNORE_TABLES 迁移为"平台域表名由实体注解声明"（@TenantIgnored）而非集中手工维护。

#### R-021 MessageSummarySummarizer 截断切断代理对【Worth】
- **涉及**：MessageSummarySummarizer L81 sb.substring UTF-16 截断（儿童 emoji 输入可能切断 surrogate pair 显示 �）
- **解决**：按 code point 截断（offsetByCodePoints）。

#### R-022 .last() 存量 6 处已登记无 CI 门禁【Worth】
- **涉及**：MybatisPlusConfig L15-18 注释登记（TrialAuth/ParentAuth/WeComOAuth/PromptVersion/StudentProfile/TeacherQuality 6 处）
- **解决**：收敛 Page 分页 + verify 脚本防新增。

### 测试/数据/部署面

#### R-023 01_schema.sql 落后 Flyway 16 版【Strong】
- **涉及**：backend/scripts/sql/01_schema.sql（头部自述"V1-V29 最终态 2026-08-02"，实际 Flyway 已 V45；缺 15+ 表）
- **问题**：参照物过期即误导——灾备重建执行它得到残缺 schema。
- **解决**：删除该脚本（Flyway 唯一源）或将生成纳入 CI（migration 合入自动重生成 + diff）。

#### R-024 种子脚本硬编码凭据 + 演示后门【Worth】
- **涉及**：02_seed_data.sql（试用咨询师 minjianq 密码 BCrypt 硬编码 + DEMO2026 演示码有效期 2027-12-31）
- **问题**：prod 执行即植入已知凭据入口。
- **解决**：按环境分支（prod 不落试用凭据）；凭据改部署时生成。
- **删除测试**：开发测试用户（disabled 结构数据）保留；试用咨询师 + DEMO2026 建议生产移除。

#### R-025 Playwright E2E 脱离 CI 灰色态【Worth】
- **涉及**：tests/e2e/specs 三个 spec（DA-04 议决 CI 移除后"维护但无人跑"）与 smoke-test.sh（bash+curl 31 断言）职责重叠
- **解决**：二选一——specs 接入可选 CI job（nightly）或删除并登记删除测试结论。

#### R-026 backup.sh 校验链缺口【Worth】
- **涉及**：backup.sh（pg_restore --list 只校验 volume daily；周/月从 staging 未校验副本 cp；restore 验证仅 users 单表 count）
- **解决**：校验统一 volume 事实源；恢复验证加扩展/表数检查。

#### R-027 init-school.sh 双轨写路径【Worth】
- **涉及**：deploy/init-school.sh（SQL 直插租户/学校/管理员——与 TenantProvisioningService 第二条写路径；角色 permission_set 两处重复；无事务）
- **解决**：收敛为后端管理 API；至少角色定义抽公共 SQL 片段。

#### R-028 verify-doc-numbers.sh 注释与实现不一致【Worth】
- **涉及**：verify-doc-numbers.sh L8-9 注释白名单"34、38-43、58"vs 代码 L97 实际 14 个编号
- **解决**：注释更新（或白名单移出脚本读 DESIGN-OVERVIEW 声明）。

#### R-029 工具链三件套脆弱接缝【Speculative】
- **涉及**：verify-config-passthrough.sh（awk 解析 compose YAML 缩进敏感 + SERVICE_VARS 硬编码 + 仅匹配 os.environ.get 形态）/ verify-model-names.sh 同构
- **解决**：compose 解析改 python yaml 或维持现状登记脆弱性（工具本身有单测质量良好）。

## 三、首要建议

**先做 R-007（consent 撤回后无法重新授权——合规敏感）→ R-010（业务日界时区）→ R-016（JwtTokenProvider 前缀剥离单点化）**：

- R-007 是产品功能断裂（撤回=永久失去重新授权能力）+ PIPL 合规面
- R-010 是隐性错误（服务器非东八区即全部错位，CounselingTimeZone 已存在零引用）
- R-016 是近三天真实事故根因（4a5cd516 平台域 500）未收敛
- 连带低成本：R-023（删过期 01_schema.sql）/ R-028（注释对齐）/ R-024（prod 种子凭据分支）

## 四、执行跟踪

| 候选 | 状态 | 备注 |
|------|------|------|
| R-001 | ⬜ 排期（前端专门批次） | 端点登记对齐（与 R-003 合并） |
| R-002 | ⬜ 待议决 | REST 语义 |
| R-003 | ⬜ 排期（前端专门批次） | 工厂单例 + TokenStorage 契约（与 R-001 合并） |
| R-004 | ⬜ 待议决 | 纯函数+单测 |
| R-005 | ⬜ 待议决 | 色板双轨 |
| R-006 | ⬜ 待议决 | DTO 生成 |
| R-007 | ⬜ 待议决 | **合规敏感，首要** |
| R-008 | ⬜ 待议决 | 脱敏守卫 |
| R-009 | ⬜ 待议决 | PIPL 删除范围 |
| R-010 | ✅ 已实施（本轮） | CounselingTimeZone.today()/todayStart() 三服务收敛 |
| R-011 | ⬜ 待议决 | 原子 upsert |
| R-012 | ⬜ 待议决 | 低风险 |
| R-013 | ⬜ 待议决 | — |
| R-014 | ⬜ 待议决 | fail-open 观测 |
| R-015 | ⬜ 待议决 | 留痕+原子 |
| R-016 | ⬜ 待议决 | 前缀单点化，首要 |
| R-017 | ⬜ 待议决 | parse 复用 |
| R-018 | ⬜ 待议决 | — |
| R-019 | ⬜ 待议决 | 字面量收敛 |
| R-020 | ⬜ 待议决 | 关联 Q-002 |
| R-021 | ⬜ 待议决 | code point |
| R-022 | ⬜ 待议决 | .last() 门禁 |
| R-023 | ✅ 已实施（本轮） | 01_schema.sql 删除 + gen-schema-snapshot.sh + CI 门禁 |
| R-024 | ⬜ 待议决 | 种子凭据 |
| R-025 | ✅ 已实施（本轮） | Playwright specs 删除（DA-04 后无人执行，smoke-test.sh 已覆盖 API 级；删除测试结论已登记） |
| R-026 | ✅ 已实施（本轮） | 周/月备份复校验（4649910，需演练验证） |
| R-027 | ⬜ 待议决 | 写路径收敛 |
| R-028 | ⬜ 待议决 | 注释对齐 |
| R-029 | ⬜ 待议决 | — |

> 议决流程：项目负责人选择候选 → /grilling 决策树 → /codebase-design 设计 → 实施 → 合并归档（his/92）。
