# 98 设计与代码全量深度审计报告

> 审计编号：doing/98（接续 his/97 分块架构审计）
> 审计日期：2026-08-13
> 审计方式：3 路独立只读审计 agent 并行（后端 Java / 前端四端 / Python 语音+部署+工程化+设计一致性），静态审计为主 + LSP/Grep 引用链交叉核实，未修改任何文件、未 commit、未运行全量测试
> 判定原则：满足设计需求 > 实打实可用 > 拒绝过度设计；代码必须可运行，禁止虚幻占位和虚假设计；生产已上线，运行态缺陷按最高优先级计；frozen/ 冻结区与 TASK-TRACKER 已登记规划项不扣分（但已存在代码质量差仍记录）
> 分块底稿：tmp/audit98/backend.md、frontend.md、python-deploy.md（过程数据，不入库）

---

## 一、审计结论摘要

**综合评分：6.2 / 10（苛刻悲观）｜ 发布就绪度：6.5 / 10**

| 分域 | 发现数 | P0 | P1 | P2 | P3 | 评分 |
|------|-------|----|----|----|----|------|
| 后端 Java（6 模块） | 10 | 0 | 4 | 2 | 4 | 6.5 |
| 前端四端 + shared | 18 | 0 | 2 | 6 | 10 | 6.0 |
| Python 语音 + 部署 + 工程化 + 设计一致性 | 11 | 0 | 1 | 3 | 7 | 6.0 |
| **合计** | **39** | **0** | **7** | **11** | **21** | **6.2** |

**核心正面结论**（实打实核实，非文档声称）：

- 后端 9 项核心设计承诺全部真实接线：CTX-Agent 上下文简报、声纹验证单值 0.70、RAG RRF 0.6/0.4 混合检索、双层输出审查、nudge 冷场决策、多租户行级隔离 fail-fast、JWT iss/aud/jti、usage_events 计量、数据保留清理。分层纪律良好（Controller 零 Mapper 注入、无空 catch 吞噬、生产零 System.out.println）。
- 前端语音/声纹/唤醒核心链路实打实：唤醒状态锁+防抖+预加载、声纹 local/remote 双模式、TTS 7 音色×8 方言矩阵 UI、浏览器兼容静默降级；shared 真复用（auth-transport 四端 / emotionMeta 三端 / replyEmotion 三方同源）；四端 68 个组件/页面/路由全部有生产引用；XSS 面零 `dangerouslySetInnerHTML` 开窗。
- Python/部署：TTS 三级降级链每级真实可达、配置深合并单源化、CI 六类门禁全部真实执行、deploy.sh 防呆（$PROJECT_ROOT 锚定 + --verify fail-closed）、17 条告警规则引用的指标全部有真实采集方。17 项设计一致性抽查：13 ✓ / 3 ⚠️ / 1 ❌。

**核心负面结论**（本次审计最大问题域 = 代码逻辑虚化度）：

- **7 个 P1 中 4 个属"看似实现实则未接线/半接线"**：ProfileEffectivenessTracker（B-01）、VoicePersonaMatcher（B-02）两个组件实现完整 + 注入 + 零调用 + 配套测试全绿——测试通过制造"已实现"假象；声纹注册 enroll 半接线（B-03）；device/report 通道无 HMAC 签名（B-04，运行态安全风险）。
- **端点单一事实源四端断层**（F-01/F-16）：parent-h5 的 endpoints.ts 常量表是死表（fillPath 零生产消费），沦为"为过审计而建的契约假象"。
- **监控存在"假绿灯"盲区**（D-01/D-10）：TTS 全引擎告警规则按序列匹配导致降级态误报 critical；voice dashscope 模式下 asr_ready 恒 True，云端 ASR 失效时健康门禁与告警双重失效。

---

## 二、P1 问题清单（7 项，优先修复）

| ID | 域 | 问题 | 证据位置 | 虚化度/过度设计 |
|----|----|------|---------|----------------|
| B-01 | 后端 | ProfileEffectivenessTracker 画像效果回收/自校准/教师摘要 5 方法完整实现，注入 SessionEndAnalyticsService 后零调用，仅 log.debug 占位——"实现完整+测试通过+从未接线"的假实现（PROF-024） | SessionEndAnalyticsService.java L113-117 | ⚠️ 虚化 + 过度设计质疑（自校准/订正回流/量表校准未经必要性验证即实现） |
| B-02 | 后端 | VoicePersonaMatcher 死代码：TtsController 注入但全 204 行零调用；安全/危机基调锁定实际由 VoiceDegradationPolicy 实现；与 VoicePersonaResolver 职责重叠（TMATCH-002） | TtsController.java L44/L103 | ⚠️ 虚化 + 重复设计 |
| B-03 | 后端 | 声纹注册链路半接线：complete() 由 reportPhase(UPLOADED) 自动触发，真实 enroll 回调未对接 voice-service（代码自注 @TODO P0-5），"任务完成"语义失真 | DeviceVoiceprintService.java L155-166 | 半接线（核心功能正确性） |
| B-04 | 后端 | device/report 通道 permitAll 无 HMAC 签名校验，生产已上线，攻击者可伪造设备上报数据破坏数据完整性（frozen/73 已登记但运行态风险高） | RouteCatalog.java L41-42 | 未实现（运行态安全风险，按"生产已上线"原则提级） |
| F-01 | 前端 | student-h5 端点契约防线弱于 teacher-web：契约测试只做单向断言，diary/relaxation/achievements 三组端点表外硬编码游离契约之外，最复杂端反而契约防线最弱 | endpoints.ts:10-12、apiContract.test.ts:71-75 | 契约防线缩水 |
| F-16 | 前端 | parent-h5 endpoints.ts 常量表是死表：fillPath/ENDPOINTS 仅被 apiContract.test.ts 消费，三个服务文件全硬编码路径零 import——"端点单一事实源"在 parent-h5 实质落空，成为仅供测试校验的快照 | parent-h5/src/services/endpoints.ts | ⚠️ 契约假象 + 过度设计质疑（维护生产不消费的常量表，本质为过审计而建） |
| D-01 | 部署 | 告警规则逻辑缺陷：MindsafeTtsAllEnginesDown 用 `tts_engine_available == 0`，gauge 有 cosyvoice/edge_tts 两标签序列，任一引擎为 0 即触发 critical——CosyVoice 降级 edge-tts 是设计内正常态，误报"全部引擎不可用"且违背 DEGRADED≠DOWN 语义 | alert-rules.yml:70 | 逻辑 bug（应改 sum()==0） |

---

## 三、P2 问题清单（11 项）

| ID | 域 | 问题 | 要点 |
|----|----|------|------|
| B-05 | 后端 | 字段加密默认关闭 | FieldEncryptionService AES-256-GCM 完整且已接线，但 enabled 默认 false，生产 PII 字段（消息摘要/教师备注/学生画像）明文落库，未成年人心理健康数据 PIPL 合规风险 |
| B-06 | 后端 | 预合成音频库未建立 | TTSFX-002 S1 场景降级实时合成，VoicePersonaMatcher 的 7 条 PRE_SYNTH_MATRIX"矩阵已定义但音频未生成"半接线 |
| F-02 | 前端 | parent-h5 契约防漂移测试盲区 | 只扫 services/index.ts，device.ts/toc.ts 新增端点不登记不拦截 |
| F-03 | 前端 | student-h5 noImplicitAny:false | 语音/声纹最复杂端类型严格度反而最低，四端四档 |
| F-04 | 前端 | teacher/admin strictNullChecks:false + useState(null) 无类型泛滥 | 状态推断 any，失去编译期保护 |
| F-05 | 前端 | fetchSosTools 死函数 | SOS 目标态工具 API 仅测试引用，SosPanel 纯静态；⚠️ 过度设计质疑：危机态孩子"目标态工具"产品价值存疑 |
| F-06 | 前端 | SOS 安全计划创建流程占位 | 注释自陈"创建流程为余量，此处静态引导"，仅一段静态话术，设计-实现长期漂移 |
| F-07 | 前端 | 儿童端静默 catch 普遍 | Achievements/RelaxationExercises/SettingsPanel `.catch(()=>{})` 吞错，与已修复的 EmotionDiary/ToolboxPanel 三套标准并存，失败后渲染空白列表区 |
| D-02 | 台账 | BEACON 数字过时 | :96 写"10 规则"实际 17 条；:92 写声纹阈值"local 0.70/remote 0.55"已收敛 0.70 单值——"明灯"可信度受损 |
| D-03 | 台账 | prepare-models.yml 注释失实 | 仍称"deploy.sh rsync 固定 --exclude models/"，实态已反转为模型随 dist 上传 + --verify |
| D-04 | 台账 | design/04 §5.5 自相矛盾 | 残留"cd.yml deploy-frontend 已接线"，与同文档 §5.3"cd.yml 已删除"矛盾 |

---

## 四、P3 问题清单（21 项）

| ID | 域 | 问题 | 要点 |
|----|----|------|------|
| B-07 | 后端 | SecurityConfig 重复 headers 配置 | 两次 .headers() 叠加式修改残留，功能生效但重复配置 |
| B-08 | 后端 | DataRetentionCleanupJob 类级 @Transactional 对 @Scheduled 不生效 | 注解误导性，清理幂等实际影响小 |
| B-09 | 后端 | SessionEndAnalyticsService 静默降级 log.debug | 生产日志级别不输出，画像效果回收失败无感知 |
| B-10 | 后端 | 量表施测代码冻结已存在 | AssessmentScoringEngine 等仅测试引用，🧊 frozen/59 登记项，不扣分仅记录 |
| F-08 | 前端 | RelaxationExercises loading 死状态 | 声明+赋值渲染未消费，叠加 F-07 失败时只剩空壳 |
| F-09 | 前端 | admin-web 12+ 端点 Record<string, unknown> | 数据契约无编译期约束，对比 teacher-web 有类型 DTO 明显虚化 |
| F-10 | 前端 | admin-web 未复用 shared ApiError | 四端唯一未收敛 toApiError 的端，错误无 code 无法业务码分支 |
| F-11 | 前端 | toc-login 验证码倒计时 interval 未清理 | 组件卸载后仍 setState |
| F-12 | 前端 | toc-privacy 删除成功提示写入 error 字段 | 成功信息红色错误样式渲染，语义错位 |
| F-13 | 前端 | vite-plugin-pwa 死依赖残留 | PWA 已停用（SW 已删）但 devDependencies 残留 |
| F-14 | 前端 | DraggableVoiceButton 渲染期读 window.innerWidth | 不纯渲染，破坏可预测性 |
| F-15 | 前端 | EmotionSelect Tailwind 类串与 shared 色值双轨 | 有注释自陈权衡，改 shared 不同步 Tailwind 类，登记漂移风险 |
| F-17 | 前端 | teacher-web SatisfactionCard 竞态 | getSatisfaction 无卸载守卫，与同文件主 load 两套标准 |
| F-18 | 前端 | XSS 风险正面基线 | 生产代码零 dangerouslySetInnerHTML，登记防回归，建议加 lint 禁入规则 |
| D-05 | Python | ser_engines `_emotion_labels[max_idx]` 无越界保护 | 模型维度漂移时 IndexError→500，非优雅降级 |
| D-06 | 台账 | STRUCTURE.md scripts 文件数失实（7 vs 9）+ subset-font 宣称有单测实无 | |
| D-07 | CI | ci.yml 注释"十四件套"过期（实际 15 个 test） | glob 全跑无遗漏，仅数字未修正 |
| D-08 | 部署 | tts/voice Dockerfile 构建策略漂移 | voice 已 BuildKit cache mount + 阿里云镜像，tts 未同步，违背 04 §13.3"对齐"承诺 |
| D-09 | 工程化 | check-commit.sh hook 无仓库级强制机制 | 无 .githooks/husky/core.hooksPath，T4 纪律依赖开发者手动安装，实际"依赖自觉" |
| D-10 | 监控 | voice /health dashscope 模式下 asr_ready 恒 True | 云端 ASR 失效（key 无效/配额耗尽）时健康门禁与 MindsafeVoiceAsrNotReady 告警双重失效——假绿灯盲区 |
| D-11 | 脚本 | subset-font.sh 僵尸 + token-usage-report.sh 半僵尸 | 零引用零测试零 CI，token-usage-report 无 cron 接线 |

---

## 五、代码逻辑虚化度专项（本次审计最大问题域）

按"设计承诺 → 代码实态"比对，共发现 **6 处虚化/半接线**：

| 类型 | 项 | 特征 |
|------|----|------|
| 假实现（实现+注入+零调用+测试全绿） | B-01 ProfileEffectivenessTracker、B-02 VoicePersonaMatcher | 配套测试通过掩盖"未接线"事实——最危险的虚化形态，测试绿≠功能存在 |
| 契约假象（死表） | F-16 parent-h5 endpoints.ts | 常量表仅供测试校验，生产零消费，端点单一事实源实质落空 |
| 半接线（代码自注未闭环） | B-03 声纹 enroll 回调、B-06 预合成矩阵 | @TODO P0-5 自认未闭环，上线运行 |
| 占位实现（注释自陈余量） | F-06 SOS 安全计划创建流程 | 仅静态引导话术 |
| 死函数 | F-05 fetchSosTools | 仅测试引用 |

**根因**：开发侧"先实现纯函数+配套测试"满足 TDD 流程，但"接线到生产链路"缺少独立验收门禁——测试覆盖率统计的是"代码被执行过"，而非"代码被生产调用"。

---

## 六、僵死代码 / 僵尸脚本清单（必须清理项）

| 项 | 域 | 状态 | 建议 |
|----|----|------|------|
| ProfileEffectivenessTracker | 后端 | 假实现（5 方法零生产调用） | 二选一：真正接线 or 删除并在设计文档标注"未实现" |
| VoicePersonaMatcher | 后端 | 死代码（match/预合成矩阵零调用） | 删除或接线，与 VoicePersonaResolver 收敛职责边界 |
| fetchSosTools | 前端 | 死函数（仅测试引用） | SosPanel 接线 or 删除 API + 设计承诺 |
| parent-h5 endpoints.ts 常量表 | 前端 | 死表（零生产消费） | 接线 fillPath or 诚实降级（删表 + 直扫源码校验） |
| RelaxationExercises loading state | 前端 | 死状态 | 删除或补 loading/空态分支 |
| vite-plugin-pwa | 前端 | 死依赖 | 移除 devDependencies |
| scripts/subset-font.sh | 脚本 | 僵尸（零引用零测试零 CI） | 移入 scripts/archive/ 或标注一次性手工工具 |
| scripts/token-usage-report.sh | 脚本 | 半僵尸（无 test/CI/cron） | 补 cron/CI 接线 or 归档 |
| SecurityConfig 重复 headers | 后端 | 垃圾代码残留 | 合并单次 .headers() 配置 |
| design/04 §5.5 cd.yml 表述 | 文档 | 死描述（cd.yml 已删） | 删残留表述 |

---

## 七、过度设计质疑清单

| 项 | 质疑 | 判定 |
|----|------|------|
| ProfileEffectivenessTracker 画像自校准/订正回流/量表冲突校准 | 不断叠加的设计未验证必要性即完整实现，结果全无人消费 | ⚠️ 过度设计 + 假实现组合，建议删除或最小化接线 |
| VoicePersonaMatcher 与 VoicePersonaResolver 职责重叠 | 两个类都在做"音色匹配"，加预合成矩阵承诺（TTSFX-002）也未落地 | ⚠️ 重复设计，建议二选一 |
| parent-h5 endpoints.ts 常量表 | 维护生产不消费的常量表 = 为过审计而建的契约假象 | ⚠️ 要么接线要么删除，拒绝双轨 |
| fetchSosTools SOS 目标态工具 | 危机态孩子是否真会使用"目标态工具"存疑 | ⚠️ 先确认产品价值再决定接线或删除 |
| deploy-metrics.sh（12 步计时+基线 mean/p90+信号分级）/ deploy-audit.sh（R1-R6 回归规则） | 单机单人低频部署场景，可观测性体系复杂度偏高，触发频率极低 | 接受（doing/72 全量 36min 教训的针对性投资，边际价值存疑但非纯装饰） |
| 17 条告警规则（含 4 条业务规则） | 有真实风险场景，指标有真实采集方 | 不质疑 |

---

## 八、设计-实现差距总表（关键承诺抽查，分域累计 50+ 项）

**后端（16 项）**：CTX-Agent ✅｜声纹 0.70 单值 ✅｜RRF ✅｜双层审查 ✅｜nudge ✅｜多租户 fail-fast ✅｜JWT ✅｜usage_events ✅｜数据保留 ✅（@Transactional 无效 B-08）｜加密 ⚠️ 已实现未启用｜画像效果回收 ❌ 虚化｜TTS 安全基调 ❌ 虚化｜预合成矩阵 ⚠️ 半接线｜声纹 enroll ❌ 半接线｜device HMAC ❌ 未实现｜量表 🧊 冻结

**前端（16 项）**：唤醒 ✅｜声纹双模式 ✅｜TTS 矩阵 UI ✅｜悬浮语音按钮 ✅｜浏览器降级 ✅｜情绪选择页 ✅｜风险预警链路 ✅｜CTX-Agent 接收 ✅｜教师端三面板 ✅｜家长端三大功能 ✅｜admin M1~M12 ✅｜情绪单一事实源 ✅｜端点单一事实源 ❌ 四端断层｜SOS 目标态工具 ❌ 未接线｜SOS 安全计划 ❌ 占位｜PWA ✅ 已停用清理（依赖残留 F-13）

**Python/部署（20 项）**：deploy.sh 唯一通道 ✅｜--verify 门禁 ✅｜模型自托管 ✅｜JaCoCo 门禁 ✅｜pytest 入 CI ✅｜docker-build-smoke ✅｜shell-tools-test ✅｜Trivy ✅｜告警 17 条 ⚠️（BEACON 写 10）｜TTS 全引擎告警 ❌ 逻辑缺陷｜告警指标接线 ✅｜双通道 ✅｜配置三层深合并 ✅｜ASR/SER 解耦 ✅｜转写即删 ✅｜三级降级 ✅｜7×8 矩阵 ✅｜非 root ✅｜声纹阈值台账 ⚠️｜cd.yml 台账 ⚠️

---

## 九、修复批次建议

- **批次 A（P1，7 项，建议本周内）**：D-01 告警规则 sum() 修正（1 行）→ B-04 device HMAC（安全）→ B-01/B-02 虚化代码裁决（接线 or 删除，含设计文档同步）→ B-03 声纹 enroll 回调 → F-16 parent-h5 死表裁决 → F-01 student-h5 契约补齐
- **批次 B（P2，11 项）**：B-05 加密启用评估（合规）→ F-05/F-06 SOS 域裁决 → F-02/F-03/F-04 类型与契约补强 → B-06 预合成矩阵裁决 → F-07 错误处理收敛 → D-02/D-03/D-04 台账修正（1 小时量级）
- **批次 C（P3，21 项）**：僵死代码清理（D-11/F-13/B-07）+ D-10 假绿灯盲区 + 其余按序
- **冻结区不扣分但提醒**：B-04（frozen/73）按"生产已上线运行态风险"原则应提级处理，不因冻结登记而降级

## 十、生产发布就绪度评估

- 无 P0 线上崩溃级缺陷；核心业务链路（对话/声纹/TTS/预警/多租户）实打实可用。
- **发布就绪度 6.5/10**：扣分主因 = 7 个 P1 中 3 个属运行态问题（B-03 声纹任务语义失真、B-04 设备通道无签名、D-01 告警误报）+ 2 个虚化代码侵蚀"设计=实现"可信度 + 2 个契约防线断层。
- 与历史基线对比：doing/95（综合 6.9 / 发布就绪 7.2）时 P0×5；本次 P0=0 说明历史审计修复有效，但虚化度问题（假实现+死表+假绿灯）成为新的系统性短板，是"测试全绿"与"功能真实可用"之间最隐蔽的差距带。

---

## 十一、待登记项（后续指令驱动，本次未执行）

1. TASK-TRACKER 头部登记本轮审计（doing/98）
2. DESIGN-OVERVIEW 编号对照 +1
3. 批次 A 实施完成后按 doing 工作流合并归档
4. D-02/D-03/D-04 台账修正涉及 BEACON/design/04/workflow 三处

---

*报告完。审计性质：只读，未修改任何项目文件。*
