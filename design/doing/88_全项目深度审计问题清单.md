# doing/88 全项目深度审计问题清单（架构与代码实态）

> 编号：88（doing 区接续；与 frozen/88 企微告警推送同号异题，文件名可区分）
> 创建：2026-08-11 | 审计方式：独立审计 agent 逐文件真实阅读（45+ 文件），所有结论附文件路径与行号证据
> 状态：**全部修复闭环（2026-08-11，commit feed6c8）**
> 定位：全项目设计与代码实现深度审计——架构合理性/代码质量/工程化规范/团队协作/逻辑虚化度五维度，以生产可用为目标，苛刻悲观口径
> 评分：**总体 7.9/10**（架构 8.0 / 代码质量 7.5 / 工程化 9.0 / 团队协作 6.5 / 虚化度 8.5）
> 审计结论：实现质量显著好于文档/台账质量；核心链路（对话/多租户/监控告警/降级）真实可用，无假 API、无伪指标；主要风险集中在**设备域安全细节**与**台账-实态漂移**

---

## 一、问题清单

### P1（严重，生产上线前必须修）

| ID | 维度 | 位置 | 问题 | 修复建议 |
|----|------|------|------|---------|
| P1-01 | 代码质量/安全 | DeviceSecurityService.java L43/L55-71 | 设备 token **无过期校验**（javadoc 声称 24h TTL，实现仅等值比较）+ **自签名**（`sign(secret, secret)` 无时间戳防重放）——固件二期接线即暴露 | ① token 内嵌 exp 校验 24h 窗口；② 签名改 `HMAC(secret, deviceCode+"\|"+expiresAt)` 或 JWT；③ 补过期/篡改/重放单测 |
| P1-02 | 安全/设备域 | DeviceService.java L95-96/L294-306 | **设备劫持链**：reportOnline 匿名可覆盖 serverUrl + pullConfig 匿名下发——伪造报告可劫持真设备（不在 frozen/73 冻结范围） | ① serverUrl 更新要求设备 secret 签名；② 首次注册后禁止匿名重写 serverUrl；③ pullConfig 对已绑定设备要求 token |

### P2（一般，尽快排期）

| ID | 维度 | 位置 | 问题 | 修复建议 |
|----|------|------|------|---------|
| P2-01 | 架构/虚化度 | SysConfigService.java | sys_config 注册表**零运行时消费者**（纯 CRUD 台账），M1 能力域名不副实 | 选 1-2 个 HOT 项接入真实消费（如降级检测开关/企微 webhook 开关）；或文档明确"审计台账"定位 |
| P2-02 | 文档-代码一致性 | MindSafeTenantLineHandler.java L30-54 vs design/03 §5.2 | IGNORE_TABLES 代码 **18 表** vs 文档 **8 表**（device/toc 域 10 表未登记）——豁免面失实误导新增表 | 03 §5.2 同步 18 表 + 分类说明（平台级/设备域/toC 域）+ "新增平台级表必须登记" |
| P2-03 | 注释矛盾 | DegradationMatrixService.java L32-34 vs L97-98 | 类头"记录型切换不改变真实档位" vs override() javadoc"即时生效"——自相矛盾 | 统一口径"记录型声明 + 写侧覆盖键预留（运行时消费见 doing/87）" |
| P2-04 | 台账失实 | TASK-TRACKER DOC-086 | admin-web 登记 25 页，实际 **17 个业务页面** | 台账改记 17 页（或注明"17 上线 + 8 设计态"） |
| P2-05 | 台账失实/文档悬空 | TASK-TRACKER DOC-098 | 声称"新增 14 号主文档"，design/ 根目录**不存在**（仅 his/14 历史版）——引用悬空 | 补建 design/14 或改记"已归档 his/14，引用指向 his/83" |

### P3（建议）

| ID | 维度 | 位置 | 问题 | 修复建议 |
|----|------|------|------|---------|
| P3-01 | 一致性 | DegradationEventDetector.java L158-161 | Prometheus 查询 URL 裸拼接（MetricsQueryService 已修 UriComponentsBuilder）——两处编码策略分叉 | 统一 UriComponentsBuilder |
| P3-02 | 虚化度 | TtsController.java L197 | status() 返回 `"cosyvoice2/edge-tts"`，tts /health 实际 `"cosyvoice-cloud"`——引擎名不符 | 透传 /health engine 字段 |
| P3-03 | 僵尸字段 | tts-service/app.py L263 | `language_mode` 废弃字段（"已废弃 v4 保留兼容"）死代码 | 删除或 `**kwargs` 吸收 |
| P3-04 | 限流缺口 | RateLimitInterceptor.java | 限流仅覆盖 chat/tts；voiceprint/verify、device/report、config/pull 公开端点无限流 | voiceprint/verify 加 IP 限流（参照 PlatformLoginGuard）；device 公开端点加宽松限流 |

## 二、僵尸/过时代码清单

| # | 文件/代码段 | 状态 | 处置 |
|---|------------|------|------|
| 1 | tts-service/app.py L263 `language_mode` | 死代码 | 删除或 **kwargs 吸收 |
| 2 | DeviceSecurityService L43 自签名 token（`sign(secret, secret)`） | 逻辑无效（零消费者） | 改造带 exp 的 HMAC/JWT（P1-01） |
| 3 | TtsController L197 旧 engine 文案 | 过时文案 | 透传 /health（P3-02） |
| 4 | degradation:override 覆盖键写侧无读侧 | **已规划**（doing/87） | 不扣分，doing/87 排期 |
| 5 | DataAnalyticsController / RecurrenceCalculator | **已规划**（BA-08/AUD-063 显式冻结） | 保留 |
| 6 | DeviceVoiceprintService UPLOADED 自动 complete | **已规划**（@TODO，doing/84 承接） | 保留，voice-service 对接后移除 |
| 7 | S1 预合成库未建立（TtsController L92 诚实标注降级态） | 文档化降级 | 文档补注或排期建立 |
| 8 | 指标/前端 API 全真实（伪指标为零、无静态渲染假象） | 干净 | — |

## 三、设计-实现差距表（摘要）

| # | 文档声明 | 代码实态 | 判定 |
|---|---------|---------|------|
| 1 | 03 §5.2 IGNORE_TABLES 8 表 | 18 表（device/toc 未登记） | 文档滞后（P2-02） |
| 2 | DOC-098 design/14 已创建 | 根目录无 14 号文档 | 台账失实（P2-05） |
| 3 | DOC-086 admin-web 25 页 | 17 业务页 | 台账失实（P2-04） |
| 4 | 监控/降级/告警全链路（OPS-MON） | 采集器/检测器/17 规则/企微触达全真实 | **一致** ✓ |
| 5 | 04 §9.3 告警 17 条 | alert-rules.yml 实查 17 条一一对应 | **一致** ✓ |
| 6 | M1 配置注册表 HOT/RESTART | 纯 CRUD 零消费 | 半成品（P2-01） |
| 7 | 设备 token TTL 24h | 无过期校验 | 代码缺陷（P1-01） |
| 8 | Python 配置契约（env/yml/代码） | deep_merge + fail-fast + CI diff 门禁 | **一致** ✓ |

## 四、修复任务归口（TASK-TRACKER §三十五）

> AUDIT-DEEP-001~011（2026-08-11 登记，见 TASK-TRACKER §三十五）：
> P1 批次：001 设备 token 过期+签名改造（P1-01）、002 reportOnline serverUrl 加固（P1-02）
> P2 批次：003 sys_config 消费接线（P2-01）、004 IGNORE_TABLES 文档同步（P2-02）、005 降级矩阵注释统一（P2-03）、006 admin-web 页数台账修正（P2-04）、007 design/14 补建或改记（P2-05）
> P3 批次：008 查询编码统一（P3-01）、009 TtsController engine 透传（P3-02）、010 language_mode 清理（P3-03）、011 公开端点限流（P3-04）
> 执行顺序：001/002 → 003 → 004~007 → 008~011；每批完成即回归
> **实施完成（2026-08-11，commit feed6c8）**：001~011 全部闭环；code-review 追加 P0-1（reportOnline 重签绕过）+ P1-1（未绑定 serverUrl 改写）+ P2-1~P2-2 + P3-1~P3-4 一并修复；回归 service 1093 + api 443 + ai 520 + tts 95 + voice 40 全绿
