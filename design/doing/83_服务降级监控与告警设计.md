# 83 服务降级监控与告警设计（方案 + SPEC）

> 状态：doing（未冻结） | 创建：2026-08-09 | 更新：2026-08-09（方案核对代码定稿 + 补 SPEC 章节 + 与后台管理端衔接整合 OPS-MON-007/008） | 触发：BUG-TTS-01 事故复盘
> 事故：CosyVoice 主引擎因 dashscope SDK API 变更 100% 失败，系统静默降级 edge-tts 长期运行，**无任何告警触达管理员**。
> 根因：① 监控栈（prometheus/alertmanager）配置存在但生产未部署；② 现有 `tts_engine_available` 指标只反映**配置层**可用性（SDK 已装 + key 非空 = 1），检测不到**运行期降级**（每次合成实际失败）。
> **定位：监控链路/数据层**（指标埋点 → 告警规则 → 通知触达 → 事件落库 → 部署演练）。与 `design/doing/83_后台管理端AdminConsole设计方案.md`（管理视图/操作层：M2 指标看板/告警中心、M3 降级矩阵/手动切换）为**衔接关系**：本文档产出指标/告警/事件数据流，管理端文档消费展示；管理端侧属监控链路的实现任务（降级事件检测落库、告警采集落库）**统一归口本文档设计实现**（OPS-MON-007/008，任务提取自管理端文档 §5.2 2.3 / §5.3 3.3）。

---

## 一、设计目标

| # | 目标 | 度量 |
|---|------|------|
| G1 | 服务**运行期降级**可观测（主引擎失效但兜底可用） | 降级事件计数 > 0 即可见 |
| G2 | 降级信号 **≤5 分钟**触达管理员 | 告警规则 for 5m + 企微推送 |
| G3 | 复用现有基建，改动最小 | 不改监控架构，只补指标埋点 + 规则 + 部署 |

## 二、现状盘点（2026-08-09 实测）

| 层 | 现状 | 缺口 |
|----|------|------|
| 指标暴露 | 后端 `/actuator/prometheus`、tts `/metrics`（`tts_synthesize_requests_total{engine}`）、voice `/metrics` 均就绪 | **无降级事件指标**（engine label 只在成功路径打点，降级路径仅 error 计数） |
| 规则 | alert-rules.yml 10 条（down/5xx/延迟/全引擎不可用/失败率/ASR/SER） | **主引擎降级规则缺失**：`tts_engine_available{engine="cosyvoice"}=1` 但实际合成 0 成功 → 无告警 |
| 通道 | Alertmanager wechat_configs 已配置（compose 注入 WECOM_*） | **WECOM_* 未配置**（.env 空值）→ 推送失败仅留日志；后端 `ALERT_WECOM_WEBHOOK_URL` 空 → 业务告警同样降级为日志 |
| 部署 | docker-compose.monitoring.yml 完整（prom 2.53.1 / alertmanager 0.27.0 / grafana 11.2.2，不公网暴露） | **生产未启动**（docker ps 无监控容器） |

## 三、方案设计

### 3.1 指标埋点（服务内，改动最小）

**TTS（tts-service）**：新增降级事件计数器，在策略层 `synthesize_with_degradation` 返回后打点：

```
tts_degraded_events_total{direction="cosyvoice->edge_tts"}   # Counter，策略降级时 +1
```

- 判定：`result.engine != 首选引擎(backends[0].name)` 且 `result.retried == false`（instruct 无指令重试成功不算降级，仍记 cosyvoice_fallback）
- **实现约束**：现有 `Metrics` 类为单 label key 结构（`_counters: label -> count`，渲染时固定 `label_key`），不支持双标签 → 新增独立 `_degraded_metrics = Metrics()` 实例，label 用 `direction` 拼接（`"cosyvoice->edge_tts"`），`/metrics` 端点追加一段 render（counter 名 `tts_degraded_events_total`、label_key `direction`；duration 传 0.0，不污染原 summary）
- 同时保留现有 `tts_synthesize_requests_total{engine}`（成功引擎分布）——降级率 = degraded / requests
- 埋点位置：app.py `synthesize()` 成功路径 `_metrics.record(engine_label, ...)` 旁（约 8 行改动）

**后端（counseling-ai）**：**已埋点，零改动**——`ResilientChatModel` 已有 `mindsafe.llm.model_fallback{from,to}` Counter（主模型异常 → 备用模型时 +1，Micrometer 导出为 `mindsafe_llm_model_fallback_total`）。本次仅补告警规则（§3.2），不新增指标（YAGNI：reason 维度可从 WARN 日志追查，不建多标签）。

**Voice（voice-service）**：已覆盖（`voice_asr_ready`/`voice_ser_ready`/`voice_analyze_requests_total{result}`），不新增。

### 3.2 告警规则（alert-rules.yml 新增 3 条）

```yaml
# 主引擎失效但兜底可用（本次事故场景：CosyVoice 100% 失败静默降级）
- alert: TtsPrimaryEngineDegraded
  expr: |
    sum(rate(tts_synthesize_requests_total{engine="edge_tts"}[5m])) > 0
    and sum(rate(tts_synthesize_requests_total{engine="cosyvoice"}[5m])) == 0
    and sum(rate(tts_synthesize_requests_total[5m])) > 0
  for: 5m
  labels: { severity: warning, team: platform }
  annotations:
    summary: "TTS 主引擎（CosyVoice）持续降级"
    description: "近 5 分钟全部合成走 edge_tts，CosyVoice 0 成功——检查 dashscope SDK 版本/API Key/配额。"

# 降级率超过 30%
- alert: TtsDegradeRatioHigh
  expr: |
    sum(rate(tts_degraded_events_total[5m]))
      / clamp_min(sum(rate(tts_synthesize_requests_total[5m])), 0.001) > 0.3
  for: 10m
  labels: { severity: warning, team: platform }
  annotations:
    summary: "TTS 降级率过高"
    description: "主引擎降级率 {{ $value | humanizePercentage }} 持续 10 分钟。"

# LLM 主备切换（服务可用但主供应商异常；复用 ResilientChatModel 现有计数器）
- alert: LlmPrimaryFailing
  expr: sum(rate(mindsafe_llm_model_fallback_total[10m])) > 0
  for: 5m
  labels: { severity: warning, team: platform }
  annotations:
    summary: "LLM 主供应商连续失败"
    description: "近 10 分钟发生 {{ $value }} 次主→备切换，检查主 Key 余额/限流/服务状态。"
```

**规则设计要点**：
- 主引擎降级规则用**成功计数**推导（cosyvoice 0 成功 + edge_tts > 0），不依赖新指标也能成立（兼容未埋点期）
- 降级率规则用新指标，阈值 30% 防抖动；`for: 10m` 避免瞬时毛刺
- 全部 warning 级（服务仍可用），critical 留给全引擎不可用（已有）

### 3.3 通知通道

| 通道 | 配置 | 触达 |
|------|------|------|
| 企微应用消息（主） | .env：`WECOM_CORP_ID`/`WECOM_AGENT_ID`/`WECOM_SECRET`/`WECOM_ALERT_TO_USER` → alertmanager wechat_configs | 秒级推送至管理员 |
| 业务告警 webhook（辅） | .env：`ALERT_WECOM_WEBHOOK_URL`（群机器人）→ 后端 WeComAlertService | SLA 类业务告警 |
| 日志留存（兜底） | 缺配置时 alertmanager 日志 + 后端降级日志（现有约定不变） | 事后审计 |

部署步骤：`.env` 补 4 个 WECOM 变量 + `GRAFANA_PASSWORD`（强密码，:? fail-fast 强制）→ `docker compose -f docker-compose.monitoring.yml up -d`。

### 3.4 验证与演练（发布后必做）

1. **冒烟**：`docker exec mindsafe-prometheus promtool check rules /etc/prometheus/alert-rules.yml` + `curl` 三个抓取端点确认 target up
2. **降级演练**：临时改 tts-service `_TTS_POLICY` 首选引擎为不可用（或断 DASHSCOPE key）→ 触发 1 次合成 → 断言 `tts_degraded_events_total` +1、5 分钟内告警推送（企微可见）→ 恢复
3. **Grafana 看板**（可选）：TTS 引擎分布、降级事件时间线、LLM 切换计数

### 3.5 降级事件检测与落库（degradation_events，衔接管理端 M3 3.3）

自动降级事件由**监控侧检测器**落库（手动切换事件由管理端 API 直接写库，见管理端文档 §7.3）：

| 项 | 设计 |
|----|------|
| 数据源 | Prometheus 指标变化：`tts_degraded_events_total`（TTS 降级）、`mindsafe_llm_model_fallback_total`（LLM 切换）；ASR/SER 待规则扩展后接入（§七 后续） |
| 检测方式 | 后端定时轮询（30s，与 service_health_snapshots 采样同频）比对指标增量：增量 > 0 → 写一条 auto 事件；按 last_value 去重，连续轮询不重复写 |
| 恢复事件 | 主引擎成功计数恢复 / 降级率回落（如 TtsDegradeRatioHigh 恢复）→ 写 to_state=恢复档位事件（trigger_type=auto） |
| 落库 | `degradation_events` 表（结构见管理端文档 §6.5）：point/from_state/to_state/trigger_type=auto/operator=null/occurred_at |
| 与手动切换的关系 | 手动切换事件 trigger_type=manual（管理端写）；检测器**跳过已 manual 覆盖的点**（Redis 覆盖键存在时不写 auto 事件，避免双写） |
| 幂等 | 检测器多实例用 Redis SETNX 分布式锁（与逾期扫描任务同机制，管理端文档 R-9）；同 point 连续降级不重复写，恢复→再降级为新事件 |
| 组件落点 | `com.mindsafe.api.ops.DegradationEventDetector`（counseling-api ops 包，与 M2/M3 API 同模块）；`@Scheduled(fixedRate=30s)`（项目已有先例：RiskNotifyRetryJob/DataRetentionCleanupJob/SlaEscalationScanner）；Mapper 复用 degradation_events（MyBatis-Plus） |

### 3.6 告警事件采集落库（alert_events，衔接管理端 M2 2.3）

M2 告警事件中心的历史查询依赖 `alert_events` 表（AlertManager 仅保留 120h），采集器归口本文档：

| 项 | 设计 |
|----|------|
| 数据源 | AlertManager API（`/api/v2/alerts`，容器内网可达，后端代理）+ 后端 AlertService 业务告警（WeComAlertService 已接线） |
| 采集方式 | 后端定时拉取（60s）AlertManager active/resolved 告警 → upsert `alert_events`（source=alertmanager）；AlertService 发出时同步写（source=alertservice） |
| 状态流转 | firing→resolved 更新 resolved_at；ack/closed 由管理端 API 更新（管理端文档 §7.2 POST /api/v1/ops/alerts/{id}/ack） |
| 落库 | `alert_events` 表（结构见管理端文档 §6.4） |
| 保留策略 | 保留 30 天，超期定时清理（与快照一致） |
| 组件落点 | `com.mindsafe.api.ops.AlertEventCollector`（counseling-api ops 包）；`@Scheduled(fixedRate=60s)`；AlertManager 访问走后端代理（内网容器地址，管理端文档 §5.2 关键决策） |

## 四、改动清单

| 文件 | 改动 | 量级 |
|------|------|------|
| backend/tts-service/app.py | 新增 `tts_degraded_events_total` 计数器（独立 Metrics 实例 + direction 单标签） | ~8 行 |
| backend/tts-service/test_app.py | 降级事件计数单测 | +1 用例 |
| backend/counseling-ai | **零改动**（`mindsafe.llm.model_fallback` 已埋点，仅复用） | 0 |
| backend/counseling-api（ops 监控采集，新增） | 降级事件检测器（§3.5）+ 告警采集器（§3.6）：定时任务 + degradation_events/alert_events 落库 | 新增 2 组件（与 M2/M3 API 同模块） |
| deploy/monitoring/alert-rules.yml | +3 条规则（主引擎降级/降级率/LLM 切换） | ~30 行 |
| deploy/.env.example | 注释 WECOM_ALERT_TO_USER/GRAFANA_PASSWORD 必填说明 | 注释 |
| 生产 .env + compose | 启动 monitoring 栈 + 配置 WECOM_* | 运维操作 |

## 五、SPEC 验收标准

| # | 验收项 | 标准 |
|---|--------|------|
| AC-1 | TTS 降级指标埋点 | 策略降级（engine≠首选 且非 retried）时 `tts_degraded_events_total{direction="cosyvoice->edge_tts"}` +1；正常合成不计数；全失败 503 不计数；instruct 无指令重试成功不计数（仍记 cosyvoice_fallback） |
| AC-2 | 指标兼容 | `/metrics` 输出合法 Prometheus 文本格式，`promtool check metrics` / curl 解析无告警；原 `tts_synthesize_requests_total{engine}` 与 summary 输出不变（回归） |
| AC-3 | 单测 | test_app.py 新增降级计数用例 + 既有用例全绿（正常路径不计数、503 不计数） |
| AC-4 | 规则语法 | `docker exec mindsafe-prometheus promtool check rules /etc/prometheus/alert-rules.yml` 通过；3 条新规则（TtsPrimaryEngineDegraded/TtsDegradeRatioHigh/LlmPrimaryFailing）加载生效 |
| AC-5 | 规则表达式 | LlmPrimaryFailing 引用 `mindsafe_llm_model_fallback_total`（与 ResilientChatModel 计数器名一致，Micrometer `.`→`_` + `_total` 后缀） |
| AC-6 | 降级演练 | 临时断 DASHSCOPE key（或首选引擎不可用）→ 触发 1 次合成 → 指标 +1 → **≤5 分钟**企微应用消息收到 TtsPrimaryEngineDegraded 告警 → 恢复 key 后指标停止增长 |
| AC-7 | 生产部署 | monitoring 栈三容器运行（prom 2.53.1/alertmanager 0.27.0/grafana 11.2.2）；3 个抓取端点（backend:8080、tts-service:10096、voice-service:10095）target up；Grafana 仅 127.0.0.1:3002 可达 |
| AC-8 | 回归 | tts-service 单测全绿；后端 `mvn verify` 不回归（counseling-ai 零改动） |
| AC-9 | 降级事件检测落库 | 触发 TTS 降级（AC-6 演练场景）→ 30s 内 degradation_events 出现 auto 事件（point=tts，from/to 正确）；连续轮询不重复写；manual 覆盖期间不写 auto 事件；恢复后写恢复事件；多实例并发不重复（SETNX） |
| AC-10 | 告警采集落库 | alert_events 与 AlertManager active 告警一致（抽样断言）；resolved 状态同步；AlertService 业务告警同步落库（source=alertservice）；保留 30 天清理任务生效 |

## 六、任务归口表（ticket）

> **审计核对注（2026-08-09）**：`metrics_common.py` `Metrics` 类确认为单 label key 结构（`_counters: label -> count`，渲染固定 `label_key`）——§3.1 独立 Metrics 实例方案成立；`ResilientChatModel` 确认为 `Counter.builder("mindsafe.llm.model_fallback").tag("from",..).tag("to",..)`——§3.1/AC-5 引用准确；alert-rules.yml 现有 10 条规则确认。

| 任务 ID | 任务 | 说明 | 状态 |
|---------|------|------|:---:|
| OPS-MON-001 | doing/83 设计文档生成 | 本文件：方案 + SPEC（核对代码现状定稿） | 🟩 本次 |
| OPS-MON-002 | TTS 降级指标埋点 | app.py 独立 Metrics 实例 + direction 标签 + test_app.py 用例（AC-1~3） | ⬜ |
| OPS-MON-003 | alert-rules 新增 3 条规则 | TtsPrimaryEngineDegraded / TtsDegradeRatioHigh / LlmPrimaryFailing（AC-4~5） | ⬜ |
| OPS-MON-004 | 生产部署 monitoring 栈 | .env 补 WECOM_*（4 项）+ GRAFANA_PASSWORD → `docker compose -f docker-compose.monitoring.yml up -d`（AC-7） | ⬜ |
| OPS-MON-005 | 降级演练 + 验证 | 断 key 触发 → 指标 +1 → 企微 5 分钟内触达 → 恢复（AC-6）；Grafana 看板（可选） | ⬜ |
| OPS-MON-006 | 合并归档 | doing/83 最终态并入 design/04 §9 监控/06 §配置章节 + 管理端文档衔接（M2/M3 数据源引用） → 归档 his/83（DOC-086） | ⬜ |
| OPS-MON-007 | 降级事件检测器 | 轮询指标增量 → degradation_events 落库（auto/恢复事件/去重/跳过 manual 覆盖点/SETNX 锁）（AC-9）；**任务提取自管理端文档 §5.3 3.3** | ⬜ |
| OPS-MON-008 | 告警采集落库 | AlertManager 拉取 + AlertService 同步 → alert_events（AC-10）；**任务提取自管理端文档 §5.2 2.3** | ⬜ |

> 登记：TASK-TRACKER.md 追加 DOC-086 任务行（本表对应任务），执行时逐条更新状态。
> **衔接**：本文档任务（OPS-MON-001~008）为管理端 M2/M3 的数据前置——OPS-MON-002/003/004/008 先于 M2 指标看板与告警中心（P1），OPS-MON-007 先于 M3 降级矩阵（P2）；管理端消费侧（UI/API/手动切换）见管理端文档 §5.2/§5.3/§7.2/§7.3。
> **执行顺序（依赖链）**：OPS-MON-002（埋点）→ 003（规则，依赖 002）→ 004（部署，依赖 003 语法验证）→ 005（演练，依赖 004）→ 007（检测器，依赖 002/004）→ 008（采集器，依赖 004）；006（归档）最后。每步完成即跑对应 AC 验收。

## 七、范围与后续

- 本次实现：TTS 降级指标 + 复用 LLM 切换指标 + 3 条规则 + 降级事件检测落库（OPS-MON-007）+ 告警采集落库（OPS-MON-008）+ 生产部署 + 演练
- 管理端衔接：M2 告警中心/指标看板消费 alert_events + Prometheus（OPS-MON-008/003）；M3 降级矩阵消费 degradation_events + 指标（OPS-MON-007/002）；手动切换（Redis 覆盖键 + manual 事件）在管理端文档 §5.3/§7.3 设计实现
- 后续可选：ASR 方言降级率、WS 推送降级（BUG-T-BASE-03 修复后补）、告警收敛/值班轮转、Grafana 看板模板化
- 不引入：新监控组件（保持 prom+alertmanager+grafana 三件套）、PagerDuty 类外部服务
