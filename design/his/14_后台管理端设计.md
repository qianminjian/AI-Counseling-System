# 14 - 后台管理端设计（AdminConsole）

> ⚠️ **已归档（2026-08-10，DOC-095 修正）**：本文件曾短暂作为主文档并列板块（DOC-098），因 doing/83 提前归档修正而撤销主文档身份，归档 his/ 只读溯源；实施态承接返回 **design/doing/83**（ADMIN tickets 跟踪不变）。

> 版本：v1.0（新增并列板块）| 创建：2026-08-10
>
> 合并来源：his/83 后台管理端 AdminConsole 设计方案（设计态，DOC-086/095）+ doing/88 实施态（DOC-098，已并入本文档后归档）+ 代码实态（admin-web / 平台域 Controller / V34~V38 迁移）
>
> 适用对象：admin-web 前端、平台域后端（Platform*/Ops/Admin*）、平台运营方、平台管理员、后续运维与合规开发
>
> 关联：01 §六（平台管理后台模块状态表）、03 §8（监控运维）、02 §6.6（平台级表）、07 §2.8.1（计量计费）、doing/86（部署验证）、frozen/38（M4 计费冻结）、frozen/87（LLM 成本）

---

## 一、定位与背景

平台级运营管理端（admin-web，`/admin/` 路径），区别于教师端（学校内管理）：平台运营方对全部租户做**运营、监控、治理与合规**。与教师端为层级关系（平台→学校→班级），学生级明细默认脱敏（R-7）。

**背景**：doing/83 完成设计态（12 能力域 M1~M12 + 调研 + SPEC），2026-08-09 合并归档（DOC-095）；P0~P3 四期 29 ticket 全部交付后，本文档为**实施态综合设计**——设计 ↔ 代码对照的单一事实源。

## 二、实施总览（P0~P3 四期）

| 期 | 内容 | ticket | 交付状态 |
|----|------|--------|---------|
| P0 | 底座：platform_admin 独立账号 + 登录端点 + ops 域 + admin-web 骨架（V35） | 8 | ✅ 完成（2026-08-09） |
| P1 | 配置与业务核心：M7 提示词中心 / M8 预警 SLA / 邀请码与批量导入（V36） | 8 | ✅ 完成 |
| P2 | 治理深化：M3 降级矩阵 / M9 统计 / M10 台账 / M12 洞察 | 7 | ✅ 完成 |
| P3 | 商业化与合规：M4 计量采集层 / M11 合规视图（V37/V38） | 6 | ✅ 完成（计费层冻结待 frozen/38） |

交付统计：后端 8 个平台域 Controller、迁移 V34~V38（8 张新表 + sys_config 种子）、前端 admin-web 15 视图（17 tsx 组件）、API 12 节。

## 三、代码架构实态

### 3.1 前端 admin-web（独立应用）

```
frontend/admin-web/src/
├── App.tsx                    # state 路由 + 登录守卫 + 角色视图分发（401/403 联动）
├── api.ts                     # admin 请求封装（PLATFORM_ token 前缀 + UNAUTHORIZED_EVENT）
├── components/AdminLayout.tsx # 布局 + allowedViews(role) 角色菜单
└── pages/                     # 15 视图
    ├── LoginPage / OverviewPage / ForbiddenPage
    ├── ConfigPage / PromptPage / RiskPage / SlaPage
    ├── LedgerPage / DegradationPage / KnowledgePage
    ├── ChannelPage / InsightsPage / UsagePage / CompliancePage
```

- 路由：state 路由（`view` 状态机，ADMIN-P0-04 决策）
- 视图 ↔ 能力域：overview（M2）/ config（M1）/ prompt（M7）/ risk+sla（M8）/ ledger（M10）/ degradation（M3）/ knowledge（M9）/ channel（M10 通知）/ insights（M12）/ usage（M4）/ compliance（M11）
- 角色双保险：后端 403 + 前端 `allowedViews(role)` 菜单过滤（ForbiddenPage）

### 3.2 后端平台域（8 Controller）

| Controller | 域 | 关键端点（实态） |
|---|---|---|
| PlatformAuthController | 认证 | POST /login（platform_admin 独立登录，PLATFORM_ token） |
| PlatformController | 总览 | /overview、/tenant-stats、/tenants/{id}、/schools |
| PlatformConfigController | M1 配置 | /registry、/{key}（GET/PUT）、/{key}/history（变更留痕） |
| OpsController | M2/M3/M8 | /services/status、/health-history、/alerts、/audit-logs、/risk/overview、/risk/sla-stats、/risk/overdue、/risk/{id}/transfer |
| AdminController | M10 邀请码/导入 | POST /batch、/{codeId}、/import-template、/import-students、/audit-logs |
| AdminPromptController | M7 提示词 | /versions（列表/详情/创建）、/{id}/activate、/deactivate、/submit、/review、/safety-phrases |
| AdminTenantController | M5 租户 | /provision、/{id}/suspend、/resume、/{id}/health |
| AdminUserController | M6 账号 | 平台用户管理（P0 backlog 余项） |

### 3.3 认证与权限（DEC-007 方案 A）

- 独立账号模型：`platform_admin` 表（与租户 users 解耦）
- 独立登录端点：`/api/v1/platform/auth/login`，token 前缀 `PLATFORM_`（不共用业务 JWT）
- 四角色：super_admin / ops_admin / audit / content_admin（`allowedViews` 映射）
- 安全：登录防爆破（PlatformLoginGuard 内存计数，5 次失败锁 15 分钟）；权限不足 403

## 四、能力域 M1~M12（设计 → 代码对照）

| 能力域 | 设计（his/83） | 代码落点（实态） | 差异说明 |
|---|---|---|---|
| M1 系统配置 | 配置注册表 + 变更留痕 | `sys_config` + `sys_config_history`；PlatformConfigController；ConfigPage | 一致；V38 种子落库 |
| M2 系统应用监控 | 三态 + 看板 + 告警中心 | `service_health_snapshots` + `alert_events`；OpsController；OverviewPage | 一致 |
| M3 服务切换降级监控 | 降级矩阵 + 运行时覆盖键 | `degradation_events`；OpsController /alerts；DegradationPage | 手动切换运行时键按 R-2 挂远期 |
| M4 租户计量计费 | 采集→计价→计费三层 | `usage_events`（V37 采集层 ✅）；UsagePage | **计价/计费冻结**（frozen/38） |
| M5 租户管理 | 生命周期 + 配额 + 详情 | AdminTenantController /provision、suspend、resume、health | P1 交付 |
| M6 平台基础 | platform_admin 四角色 | `platform_admin` 表 + PlatformAuthController | 与 DEC-007 方案 A 一致 |
| M7 提示词中心 | 编辑 + 审核流 + 安全话术只读 | AdminPromptController /versions 全流程 + /safety-phrases；PromptPage | P1 交付（用户核心） |
| M8 业务信号与预警 | 风险全景 + SLA + 逾期升级 | OpsController /risk/* + `sla_escalation_log`（V36）；RiskPage + SlaPage | P1 交付 |
| M9 知识库管理 | 内容管理 | KnowledgePage | P2 交付 |
| M10 通知与台账 | 渠道 + 审计台账 | ChannelPage + LedgerPage；/audit-logs | P2 交付 |
| M11 数据安全合规 | 合规中心 | CompliancePage | P2/P3 交付 |
| M12 运营洞察 | 洞察分析 | InsightsPage | P2 交付 |

## 五、数据模型（V34~V38）

| 迁移 | 表 | 用途 |
|---|---|---|
| V34 | `degradation_events`、`alert_events` | 降级/告警事件（M3/M2） |
| V35 | `platform_admin`、`service_health_snapshots` | 平台账号 / 健康快照（M2） |
| V36 | `sys_config`、`sys_config_history`、`sla_escalation_log` | 配置注册表+留痕 / SLA 升级日志；含 `prompt_versions.status` 扩展 |
| V37 | `usage_events` | 计量采集层（M4，计费冻结） |
| V38 | `sys_config` 种子 | 配置默认值种子 |

> **口径修正**：设计态登记"新增 10 张表"实为 **8 张新表 + `prompt_versions.status` 字段扩展**。

## 六、部署与验证

- admin-web 独立部署（/admin 路由，nginx alias），随 deploy.sh 发布
- 部署验证项由 doing/86 跟踪（生产监控栈部署后逐项闭环）
- 2026-08-10 实测：admin 登录 → 数据大屏（teacher-web 角色裁剪，design/35）→ 管理控制台（邀请码管理/批量导入 CSV）→ 平台总览（学校/学生/会话统计）全部通过（UI-TEST-004 T-07 补测）

## 七、冻结与后续

1. **frozen/38**：M4 计价/计费层（rate_plans/subscriptions/billing），解冻后按 §四 M4 实施
2. **frozen/87**：LLM 成本跟踪与 DeepSeek 涨价应对（87-01~04），与 M4 用量数据衔接
3. **R-2 服务操作执行通道**：P0 只读展示 + SSH 人工（受限命令执行挂远期）
4. **doing/86**：生产监控栈部署验证项

## 八、验收标准

| # | 验收项 | 标准 |
|---|--------|------|
| 1 | 实态对照 | M1~M12 均有代码落点（页面/端点/表），与代码 grep 一致 |
| 2 | 口径修正 | 表（10→8+字段）、页面（25→15 视图）差异显式记录 |
| 3 | 可溯源性 | 承接 his/83 + DOC-095，冻结项指路 frozen/38/87 |
| 4 | 主文档联动 | 01 §六 / 03 §8 / 02 §6.6 / 07 §2.8.1 引用本文档 |

---

## 附：与主文档体系的衔接

- **01 §六**：平台管理后台模块状态表（十二能力域交付态）——本文档为其详细设计
- **03 §8**：监控运维（告警规则/降级链路实态表）——M2/M3 数据源
- **02 §6.6**：平台级表（V34~V38）——数据结构单一事实源为迁移文件
- **07 §2.8.1**：计量计费三层（M4 采集层落地，计价/计费冻结）
- **his/83**：设计态只读溯源（调研/SPEC 过程数据）
