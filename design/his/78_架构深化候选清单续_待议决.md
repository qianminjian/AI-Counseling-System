# doing/78 架构深化候选清单（续：待议决）

> 状态：✅ 已实施（2026-08-08，全部 14 候选完成；实施记录见 §5）
> 登记编号：DOC-073 续（2026-08-08 合并 doing/77 时拆出未议决候选，保持编号关联）
> 详细分析：各候选 Problem/Solution/Benefits 完整分析见 `design/his/77`（§3-§19，只读溯源）；本文件仅保留决策入口

---

## 1. 待议决候选总览

| # | 候选 | 强度 | 一句话 | 建议批次 |
|---|------|------|--------|---------|
| B2 | nudge 暖场策略双真值常量漂移（Lua vs 快照） | 🟡 Worth | T5 引入的双轨遗留，改阈值需改三处 | 与 B1 同批（对话安全域） |
| B3 | RedisChatMemory：僵死 KEYS 方法 + 无租户 key + 元数据丢失 | 🟡 Worth | 死代码 + 生产反模式 + 跨进程契约无版本 | 与 B2 同批 |
| B4 | LLM 文案硬编码 Java（buildGenderStyle 70+ 行）绕过版本路由 | 🟡 Worth | 复用现有 PromptVersionService 零新机制 | 独立小批 |
| B5 | TeacherService 分页安全化残留 + 班级范围查询五重复 | 🟡 Worth | 纪律钩子拦不到 Service 内部 LIMIT 回流 | 独立小批 |
| B6 | buildEmotionSuggestion 绕过 DC-008 词表（3/9 码值） | ⚪ Speculative | 单点文案，收编成本最低 | 随 B4 或 F4 |
| F3 | teacher-web 轮询生命周期 ×4 | 🟡 Worth | setInterval+hidden+cleanup 四套 | 独立小批 |
| F4 | 情绪展示映射 ×4（前端镜像 DC-008） | 🟡 Worth | 键空间不统一（英文 code vs 中文 label） | 随 B6 联动 |
| F5 | TTS 降级朗读 ×3 | 🟡 Worth | 第三份实现游离在既有 seam 之外 | 随 F3 |
| F6 | 麦克风 PCM 采集启动 ×2 | ⚪ Speculative | 权限生命周期语义各自演化 | Speculative，随 F2 |
| D2 | voice-service 测试空白 + e2e 脚本失实 | 🟢 Strong | 与 tts 测试深度反差，import hack 才可测 | 随 D1 同批 |
| D3 | 配置透传契约只覆盖 TTS（voice 三变量死配置） | 🟢 Strong | 机制覆盖一半服务 | 随 D1 同批 |
| D4 | deploy.sh 重试/SSH/.env 解析散落 | 🟡 Worth | 同一概念三处变体 | 独立小批 |
| D5 | tts /health DEGRADED 语义无下游消费者 | 🟡 Worth | 降级状态被系统性丢弃 | 随 D4 |
| D6 | nginx 双轨 + 部署角落残留（GHCR/硬编码 schema） | 🟡 Worth | 宿主配置无版本化 | 随 D4 |

## 2. 实施要点（议决后展开）

| 候选 | 实施要点 | 建议批次 |
|------|---------|---------|
| B2 nudge | 配置源 `mindsafe.conversation.nudge-*` + Lua 渲染注入 + 快照引用同源；或删除 canNudge 快照 | 与 B1 同批（对话安全域） |
| B3 RedisChatMemory | 删 findConversationIds（无调用者）；serializeMessage 加 schema 版本字段；key 加租户段 | 与 B2 同批 |
| B4 LLM 文案 | prompts/ 模板 + PromptVersionService 路由；FALLBACK_MESSAGE 同迁 | 独立小批 |
| B5 TeacherService | 3 处 .last("LIMIT")→selectPage；班级范围查询抽 SessionAccessService | 独立小批 |
| B6 情绪文案 | buildEmotionSuggestion 改 labelOf 拼接 | 随 B4 或 F4 |
| F3 usePolling | teacher-web hooks/usePolling + 4 处替换 | 独立小批 |
| F4 emotionMeta | shared emotionMeta 模块 + 4 处消费 | 随 B6 联动 |
| F5 browserSpeak | VoiceLoginOverlay/RelaxationExercises 复用 useTtsPlayer | 随 F3 |
| F6 麦克风会话 | 采集会话模块 + 3 处消费 | Speculative，随 F2 |
| D2 voice 测试 | ASR seam + 纯函数测试 + e2e 脚本整改 | 随 D1 同批 |
| D3 透传契约 | 变量清单泛化 + voice 三变量登记透传 | 随 D1 同批 |
| D4 deploy.sh | 重试执行器 + SSH 参数收敛 + .env source 单点 | 独立小批 |
| D5 health | /health DEGRADED 非 200 或 service-manager 读 body 告警 | 随 D4 |
| D6 nginx/残留 | 单轨化 + compose 锚点 + 清 CD 残留 + restore.sh 参数化 | 随 D4 |

## 3. Speculative 探索备忘

- **B6**：收益最小但成本最低，随 B4 或 F4 顺带；不单独立项
- **F6**：依赖浏览器权限流细节，需实测后评估；探索方式=对比三处实现差异清单
- **D6 宿主 nginx**：需现场确认生产宿主配置是否已从仓库同步（Speculative 成分），单轨化前置调研

## 4. 溯源

- 候选证据锚点行号为 2026-08-08 审计时快照（his/77 §1-§19），实施前以代码现状为准（参照 his/76 §3.3 教训：审计行号会随重构失效）
- 已议决并实施：B1（his/77 §22，落点 09 §3.11/§5.14.3）、D1（his/77 §23，落点 06 §4.0）、F1+F2（his/77 §24，落点 03 §2.7.3）
- HTML 可视化报告：`tmp/architecture-review-20260808-074829.html`（已 git 忽略，不入版本库）

---

## 5. 实施记录（2026-08-08，/implement 七批）

> 范围：本文件全部 14 候选（B2-B6 / F3-F6 / D2-D6），与 his/77 §27（B1/D1/F1+F2）衔接后 doing/77+78 全部候选闭环。

| 任务 | 结果 | 证据 |
|---|---|---|
| B2 nudge 配置单源 | ✅ | NudgeProperties（maxCount/minIntervalSeconds 下沉 application.yml）+ Lua 渲染注入 + canNudge 快照引用同源；删除双真值（grep 验收仅一处定义） |
| B3 RedisChatMemory 收编 | ✅ | 删僵死 findConversationIds（无调用者）；serializeMessage 加 schemaVersion 字段（兼容反序列化）；key 加租户段（TenantContextHolder 同请求线程绑定） |
| B4 文案模板化 | ✅ | buildGenderStyle 70+ 行删除 → prompts/style/ 9 模板 + fallback/ 降级话术，PromptVersionService 版本路由；模板内容与旧 Java 硬编码逐字比对一致（抽查） |
| B5 TeacherService 分页安全化 | ✅ | 3 处 .last("LIMIT") SQL 拼接全部移除 → MyBatis-Plus selectPage（PaginationInnerInterceptor 已注册）；listClassStudents 下沉 SessionAccessService（tenantId 强制内置，classScope 空返回空列表） |
| B6 情绪文案收编 | ✅ | buildEmotionSuggestion 改 EmotionVocabulary.labelOf 拼接（全码值覆盖替代 3 码值 switch）；未知码值兜底「未知」（review 修正：词表不收录非 SER 码值，兜底保留在消费点） |
| F3 usePolling | ✅ | teacher-web hooks/usePolling（fnRef 最新闭包 + hidden 暂停 + cleanup）+ 4 处替换（Dashboard/BigScreen/TodayTodoPanel/useAlertWebSocket） |
| F4 emotionMeta | ✅ | shared/emotionMeta.ts 单源（镜像后端 ZH_LABELS）+ 4 处消费（EmotionDiary/EmotionSelect/MessageBubble/teacher emotionLabels）；键空间统一为英文 code |
| F5 browserSpeak | ✅ | browserSpeak.ts 抽取 + VoiceLoginOverlay/RelaxationExercises 复用 useTtsPlayer（第三份实现并入既有 seam）；double-resolve 幂等保护 |
| F6 麦克风会话 | ✅ | micSession.ts 采集会话模块 + 3 处消费（useAudioRecorder/useWakeWord/VoiceLoginOverlay）；getMicStream 统一约束 + mapMicError；释放单点 stop() |
| D2 voice 测试补全 | ✅ | ASR seam（asr_engines.py：ASRBackend ABC + DashScope/FunASR 适配器 + 3 纯函数）；测试 22→23 项（review 修正 High-1 后新增回归）；e2e 脚本 git mv 转 pytest 冒烟（无 key skip） |
| D3 透传契约泛化 | ✅ | verify-config-passthrough.sh 重写：per-service 声明清单 + 双向校验 + 代码消费校验（56 项全绿）；voice 三变量（PROCESS/ANALYZE_TIMEOUT/CORS_ORIGINS）登记 .env.example + 双 compose 透传 |
| D4 deploy.sh 收敛 | ✅ | 三处重试变体 → 统一 retry 执行器（递增退避/固定间隔 + stderr 告警不污染 stdout）；SSH_OPTS 复用（check_nginx_paths 追加 BatchMode）；service-manager .env 读取收敛 load_env_var 单点 |
| D5 health 消费 | ✅ | service-manager tts 分支读 /health body 消费 DEGRADED：cosyvoice-cloud=健康；edge-tts/none=告警但返回 0（降级≠宕机）；接口不可达 SystemExit(2) 判不健康（原语义保留） |
| D6 部署残留清理 | ✅（部分） | restore.sh TENANT_SCHEMA 参数化（默认 tenant_template）；setup-server.sh GHCR login 提示/Next steps 清理 + 步骤编号修正。**跳过**：compose YAML 锚点（三份 compose environment 差异是环境语义差异——dev placeholder/test deepseek-chat/prod 空值，非复制漂移；YAML 锚点无法跨文件共享）；nginx 双轨单轨化（需现场确认生产宿主配置，Speculative 不动生产） |
| code-review 修正 | ✅ | High-1：DashScopeASRBackend 改 dashscope.audio.asr 子模块显式导入（真实包顶层不暴露 Recognition，实测）+ api_key 显式注入 + 回归测试（sdk 无顶层属性仍可转写）；B6 unknown 兜底「未知」；AiChatServiceImpl 死字段 promptTemplateService 清理（含 2 测试）；LlmStreamEnhancer 缩进 |
| 全量回归 | ✅ | 后端 2 模块 1383 项 0 失败（ai 508 / service 875）；前端三端 1221 项（student 873 / teacher 223 / parent 125）+ tsc/oxlint 0 errors；Python 110 项（voice 33+1skip / tts 77）；bash -n 6 脚本 + retry/load_env_var 行为测试 11 项 |
| git 提交 | ✅ | 原子提交（台账登记 + 七批实施，check-commit 纪律） |

### 5.1 已知取舍（待产品确认，非阻断）

- **EmotionDiary 「双平静」**：F4 收编后 calm/neutral 同译「平静」（DC-008 词表粒度，后端本就同译），情绪日记选项页出现两个同文本选项（emoji 不同）；anxious 由「焦虑」改「紧张」（DC-008 儿童友好权威翻译，全系统统一）。测试已显式断言。若产品认为选项页需区分，可在组件层保留语境化标签（违背单源）或合并码值（影响采集数据），均需产品决策。
- **D6 compose 锚点 / nginx 单轨**：见上表 D6 行。

