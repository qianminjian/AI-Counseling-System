# 06 - 系统配置与外部服务依赖设计

> 版本：v3.1（合并版）| 整合：2026-07-28 | **追加同步 2026-08-02**（声纹双模式定稿 + CTX-Agent 上下文简报上线 + TTS 7 音色 8 方言 + 唤醒深化 + WASM/SIMD/SAB 兼容性）| **追加同步 2026-08-06**（LLM 主备配置通用化 doing/63 + 字段加密开关 doing/64 合并定稿）| **追加同步 2026-08-08**（Python 配置加载深合并单源化，doing/77 合并，见 §4.0）
> 合并来源：`57_配置统一纳管设计`（已实施 CFG-001~008）、`56_TTS音色矩阵与方言设计`（已实现 v4 双模式）
> 核心结论：**分层配置架构**（.env 密钥层 → application.yml/config.yaml 服务层 → API 下发前端运行时层），改配置不改代码；不引入配置中心（YAGNI）

---

## 1. 配置分层架构

### 1.1 设计原则

| 原则 | 说明 |
|------|------|
| 密钥不落文件 | API Key/密码/Token 只经环境变量注入，配置文件仅写 `${ENV_VAR}` 占位 |
| 单一事实源 | 每个配置项有且仅有一个权威定义处，其他层 API 获取或构建注入 |
| 分层分治 | 基础设施层（.env）→ 服务层（各服务配置文件）→ 运行时层（API 下发前端） |
| 改配置不改代码 | 业务参数变更只改配置，不触发代码构建 |
| YAGNI | 不引入 Consul/etcd/Nacos，当前规模文件 + API 足够 |

### 1.2 目标架构

```
┌─────────────────────────────────────────────────────────┐
│            deploy/.env（密钥层）                          │
│  DB_PASSWORD / JWT_SECRET / API Keys / ENCRYPTION_KEY    │
│  仅存敏感凭证，docker-compose 注入各容器                  │
└──────────────────────────┬──────────────────────────────┘
                           │ 环境变量注入
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌──────────────┐  ┌────────────────┐  ┌───────────────────┐
│ Java Backend │  │ tts-service    │  │ voice-service     │
│application.yml│  │ config.yaml   │  │ config.yaml       │
└──────┬───────┘  └────────────────┘  └───────────────────┘
       │ GET /api/v1/system/config（运行时下发，5min 缓存）
       ▼
┌─────────────────────────────────────────────────────────┐
│ 前端（student-h5 / teacher-web）                         │
│ 启动时拉取 → 覆盖本地默认值 → 运行时使用                 │
│ 本地 config/*.ts 保留为 fallback（离线/接口失败降级）    │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 配置全景索引（权威定义处）

| 配置项 | 权威定义处 | 消费方 | 变更方式 |
|--------|-----------|--------|--------|
| DB/Redis/JWT/加密密钥 | `deploy/.env` | docker-compose → 各容器 | 改 .env + 重启 |
| LLM 模型/Key（主 + 备） | `.env` + `application.yml` | Java 后端 | 改 .env + 重启（**主** LLM_PRIMARY_* / **备** LLM_BACKUP_*，任意两个 OpenAI 兼容供应商，2026-08-06 通用化，详见 §3.4） |
| 声纹阈值 0.70 单值 | `application.yml`（remote 权威）+ API 下发（local 派生） | 前端 local + 后端 remote | 改 yml + 重启（S4-1 已收敛，无双值） |
| 声纹模式 local/remote | `application.yml` | Java 后端 + 前端（间接） | 改 yml + 重启 |
| 唤醒词模型/参数 | `application.yml` → API 下发 | 前端 | 改 yml + 重启 |
| 引导脚本话术 | `application.yml` → API 下发 | 前端 | 改 yml + 重启（无需前端发版） |
| TTS 音色矩阵（7 音色） | `tts-service/config.yaml` | tts-service | 改 yaml + 重启容器 |
| TTS 模型名 | `config.yaml`（env `DASHSCOPE_TTS_MODEL` 覆盖） | tts-service | 改 env 或 yaml |
| 方言配置（native/instruct 双模式） | `tts-service/config.yaml` | tts-service | 改 yaml + 重启容器 |
| 原生方言音色映射 / 情感 Instruct 映射 | `tts-service/config.yaml` | tts-service | 改 yaml + 重启容器 |
| ASR 引擎选择 | `.env` → `voice-service/config.yaml` | voice-service | 改 .env + 重启 |
| SER 情感分析开关 | `.env` → `voice-service/config.yaml` | voice-service | 改 .env + 重启 |
| ASR/SER 模型名 | `voice-service/config.yaml` | voice-service | 改 yaml + 重启容器 |
| 密码策略/SLA/数据保留 | `application.yml` | Java 后端 | 改 yml + 重启 |
| 数据保留期（`mindsafe.retention.data-retention`） | `application.yml`（normal-session-days=180 普通 / high-risk-session-days=365 高风险，cleanup-cron 每日 03:00，DataRetentionCleanupJob 消费） | Java 后端 | 改 yml + 重启；612ef65 已从 30 修正为 180 对齐设计值 |
| SMS/企微/告警凭证 | `deploy/.env` | Java 后端 | 改 .env + 重启 |
| CORS/端口/Nginx/SSL | `deploy/nginx/` + `.env` | Nginx | 改 conf + reload |
| 前端唤醒词变体列表 | `config/wakeWord.ts`（本地保留） | 前端 | 发版（低频，可接受） |

### 不应该纳入统一配置的项目

| 项目 | 理由 |
|------|------|
| 唤醒词变体列表（WAKE_PATTERNS） | 纯前端算法逻辑，与模型强绑定 |
| 幻觉检测规则（isHallucination） | 算法逻辑，非配置 |
| ONNX WASM 路径/线程数 | 底层运行时参数，与 Transformers.js 版本强耦合 |
| IndexedDB 库名/版本 | 前端存储结构，变更需配合数据迁移 |
| 音频采样率/段时长 | 与模型输入强绑定 |
| Nginx 路由规则 | 基础设施层，独立管理 |

---

## 3. Java 后端配置（application.yml）

### 3.1 `mindsafe.system-config` 节点（下发前端）

```yaml
mindsafe:
  system-config:
    voiceprint:
      verify-threshold: ${mindsafe.voiceprint.verify-threshold}   # S4-1 占位符派生（O 专题）：单一事实源，改权威值一处生效
      max-templates: 8
      enroll-segments: 3
      verify-segments: 2
    wake-word:
      model-id: "onnx-community/whisper-tiny"
      window-seconds: 2.0
      silence-rms-threshold: 0.03
    tts:
      default-persona: "xiaoxing"
      personas: ["xiaoxing","bobo","yueliang","xiaotaiyang","dashu","doudou","qiqiu"]
    guide-scripts:
      verify: [{prompt, hint, duration}...]   # 声纹验证引导话术
      enroll: [{prompt, hint, duration}...]   # 声纹录入引导话术
```

### 3.2 下发 API

- `GET /api/v1/system/config`（permitAll，无需鉴权）
- 返回 system-config 子树 JSON；响应头 `Cache-Control: public, max-age=300`（5 分钟缓存）
- 仅下发非敏感业务参数，密钥/内网地址绝不下发

### 3.3 声纹阈值分模式管理（S4-1 收敛后：单一事实源 + 模式语义注释）

| 位置 | 当前值 | 用途 | 设计理由 |
|------|--------|------|----------|
| `mindsafe.voiceprint.verify-threshold` | **0.70** | remote 模式后端比对（`/api/v1/voiceprint/verify`）——**权威值**（AUD-001：原 0.55 过低已废弃） | 全链单一事实源，与 local 端一致 |
| `mindsafe.system-config.voiceprint.verify-threshold` | `${mindsafe.voiceprint.verify-threshold}` | 前端 local 模式比对（声纹不出设备），**占位符派生**（O 专题 S4-1） | 派生自权威值，无双值（S4-1 已收敛 0.70 单值） |
| `mindsafe.voiceprint.mode` | `${MINDSAFE_VOICEPRINT_MODE:local}` | 模式切换（`local` / `remote`） | 默认 local，隐私优先 |

**声纹登录实现细节**（commit e6f86ab + 7c29c18 + 5c6e399）：

- **前端模型**：wespeaker ONNX（256-dim embedding）从 `/mindsafe/models/wespeaker/` 同源加载（`WAKE_MODEL_REMOTE_HOST='SAME_ORIGIN'`，§6.2）
- **本地模式**：浏览器内 `wespeaker` 提取 embedding → IndexedDB 存储（`frontend/student-h5/src/utils/voiceprintStore.ts`，无网络上传）→ 登录时 cosine 比对 ≥0.70
- **远程模式**：浏览器内 `wespeaker` 提取 embedding → 上传服务端 → V28 表 `tenant_template.voiceprint_embeddings`（JSONB + sample_index）→ 登录时服务端 cosine 比对 ≥0.70
- **隐私**：仅存 256-dim 特征向量，不存原始音频，不可逆向还原声音（对齐 22 §6.3 "如实区分披露"）
- **交互**：按钮显式触发（无被动监听）+ CosyVoice xiaotaiyang/longanyang 音色引导 + 复述式话术（commit c21091d）
- **SQL 隔离**：登录调用经 `TenantContextHolder.runAsSystem` 显式声明（修复 M1-003 跨租户报错，commit c2cfc05）

**两套阈值有意不同**：这是设计选择而非 bug，是 remote 模式对真实传输损耗的承认。不应将两个阈值统一为零一一个中间值。

### 3.4 LLM 主备模型配置（LLM_PRIMARY_* / LLM_BACKUP_*，doing/63 合并定稿 2026-08-06）

**主/备可为任意两个 OpenAI 兼容供应商**，配置命名通用化（key 主 / key 备），不再绑定 DeepSeek：

| 环境变量 | 默认值 | 说明 |
|---------|--------|------|
| `LLM_PRIMARY_API_KEY` | 必填（回退链：`LLM_API_KEY` → `DEEPSEEK_API_KEY`） | 主模型凭证 |
| `LLM_PRIMARY_BASE_URL` | `https://api.deepseek.com`（回退 `LLM_BASE_URL`） | 主模型端点 |
| `LLM_PRIMARY_MODEL` | `deepseek-v4-flash`（回退 `LLM_MODEL`） | 主模型名 |
| `LLM_PRIMARY_TEMPERATURE` / `LLM_PRIMARY_MAX_TOKENS` | 0.7 / 2048 | 主模型采样参数 |
| `LLM_PRIMARY_EXTRA_BODY` | 空 | 主模型专属请求体参数（JSON），如 `{"enable_thinking":false}` |
| `LLM_BACKUP_API_KEY` | 空 = 单模型模式（回退 `LLM_FALLBACK_API_KEY`） | 备模型凭证 |
| `LLM_BACKUP_BASE_URL` / `LLM_BACKUP_MODEL` | 空（回退 `LLM_FALLBACK_*`） | 备模型端点/模型名 |
| `LLM_BACKUP_TEMPERATURE` / `LLM_BACKUP_MAX_TOKENS` | 0.7 / 2048 | 备模型采样参数 |
| `LLM_BACKUP_EXTRA_BODY` | 空 | 备模型专属请求体参数 |

**旧变量名处置（LLM-GEN-012）**：`DEEPSEEK_API_KEY` / `LLM_FALLBACK_API_KEY` / `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` **代码层保留嵌套回退**（旧部署不破），**部署层不再使用**——新装 `.env`/`.env.example` 只教新名；服务器 `.env` 已清理冗余（2026-08-06，备份 `.env.bak-cleanup-20260806`）。

**装配结构**：

- **主模型**：`spring.ai.openai` 段读 `LLM_PRIMARY_*`——`AiConfig` 手动构建（`OpenAiApi.builder().baseUrl(...).apiKey(...)` + `OpenAiChatModel.builder()`，与备对称，摆脱自动配置依赖）；embedding 独立走自动配置（`EMBEDDING_*`，回退链含主模型二级别名，旧名部署 embedding 不破）
- **备模型**：`mindsafe.llm.backup` 段读 `LLM_BACKUP_*`（原 `mindsafe.ai.fallback` 段改名，旧键别名保留）；`LLM_BACKUP_API_KEY` 非空才启用双模型降级——`ResilientChatModel` 主失败 → 备 → 安全话术，指标 `mindsafe.llm.model_fallback` 计数 +1
- **供应商专属参数**：`LlmExtraBodyConfig`（原 `DeepSeekThinkingConfig` 通用化）单个 RestClient 拦截器按 base-url 匹配注入 EXTRA_BODY；**主 base-url 含 `deepseek` 且未配 EXTRA_BODY 时自动注入 `{"enable_thinking":false}`**（行为兼容，首 token 延迟不回归）；非法 JSON 安全降级不阻断（C4 留痕）

**行为兼容矩阵**：

| 配置组合 | 行为 |
|---------|------|
| 仅 `LLM_PRIMARY_API_KEY`（或旧名回退） | 单模型模式（改造前等价） |
| `LLM_PRIMARY_*` + `LLM_BACKUP_*` | 双模型降级（任意双供应商，LLM-GEN-002/003） |
| 新旧名混配 | 新名优先 |
| 主 base-url 非 deepseek | 不注入任何专属参数（除非显式 EXTRA_BODY） |

**已验证组合**：A = DeepSeek(主) + 阿里云百炼(备，qwen-plus)；B = GLM(主) + Kimi(备)。生产实测（2026-08-06）：主=deepseek-v4-pro、备=MiniMax-M3，SSE 对话流式真实调用主模型成功。

**决策记录**：D1 备模型命名 `BACKUP`（贴合降级语义，避免与数据库主从混淆）；D2 DeepSeek 自动注入保留（行为兼容免配置回归）；D4 主模型彻底手动构建（单一事实源，embedding 仍自动配置）。Spec 编号 LLM-GEN-001~012，验收标准见 `design/his/63_LLM主备配置通用化设计.md`。

### 3.5 字段加密开关（ENCRYPTION_ENABLED，doing/64 合并定稿 2026-08-06）

**决策（2026-08-06 项目负责人）：数据加密视为尚未接线，`ENCRYPTION_ENABLED=false` 为终态**；未主动提出该专题前，不再纳入任何分析、待办或迁移规划（frozen/60 关联解除，商业化启用另行解锁）。

```yaml
mindsafe:
  encryption:
    enabled: ${ENCRYPTION_ENABLED:false}   # 系统级开关，默认关闭
    key: ${MINDSAFE_ENCRYPTION_KEY:}        # 启用时必填：Base64 32 字节
    key-version: ${MINDSAFE_ENCRYPTION_KEY_VERSION:1}
    previous-keys: ${MINDSAFE_ENCRYPTION_PREVIOUS_KEYS:}  # 轮换期历史密钥
```

**开关语义（唯一裁决者，与 profile 解耦）**：

| enabled | 密钥要求 | 加解密行为 | fail-fast |
|---------|---------|-----------|-----------|
| `false`（缺省/空/false，默认） | 不要求（可不配置） | encrypt/decrypt 纯透传（明文） | 无 |
| `true` | 必须：非空 + Base64 解码恰好 32 字节 | AES-256-GCM 加密 / 按版本解密 | 密钥缺失或非法 → 拒绝启动 |

**防呆**：enabled 缺省但 key 已配置 → 启动 WARN「密钥已配置但加密未启用（ENCRYPTION_ENABLED=false），数据以明文落库」——防止误配后静默明文。

**实现要点**：`FieldEncryptionService` 构造器新增 `enabled` 开关（默认 false，encrypt/decrypt 透传分支）；旧「prod 强制加密」行为废除（prod 无 key 不再崩溃，正常启动明文——试运行期阶段性决策）；密文格式 `v{version}:<base64>`、密钥版本化/轮换（previous-keys）、服务接口签名（encrypt/decrypt/isEncrypted/reEncrypt）均不变；存量明文数据（无 `v` 前缀）decrypt 原样返回，加密仅对新写入生效（ENC-005）。Spec 编号 ENC-001~008，验收标准见 `design/his/64_ENCRYPTION_KEY启用开关设计.md`。

**商业化解锁清单**（frozen/60 COMP-008 预登记，触发=商用发布决策）：`ENCRYPTION_ENABLED=true` 写入生产 .env → `openssl rand -base64 32` 生成密钥写入 `ENCRYPTION_KEY` 并离线备份（密钥丢失 = 历史密文不可恢复）→ 密钥轮换演练（previous-keys 装载 → reEncrypt 存量）→ `.env` 权限 chmod 600 + 访问审计 → 存量明文兼容验证（ENC-005）。

---

## 4. TTS 服务配置（tts-service/config.yaml）

### 4.0 配置加载机制（深合并单源化，tts/voice 共用，DOC-073 D1 合并 2026-08-08）

> 源：doing/77 §23（最终态，已归档 his/77）。两服务各放一份 `config_loader.py`（实现相同 + 各自测试）：

- **加载优先级**：env > config.yaml > 代码最小兜底（原 `_DEFAULT_CONFIG` 全量默认矩阵删除）
- **深合并语义**：dict 递归合并（嵌套 partial 覆盖）/ list 整体替换 / 空 dict 保守保留（无变更）/ None 显式置空（整体替换）——修复原浅合并「只配一个方言丢默认矩阵」事故隐患
- **权威单源**：矩阵类数据（tts voice_personas / voice emotion_labels）以 config.yaml 为权威载体，代码兜底仅保留运行必需字段（asr/ser 模型名等，与 tts model 兜底对称）
- **删除测试翻转**：删 config.yaml 后服务行为改变（原浅合并 + 全量默认矩阵 = yaml 零增值）
- 验收：tts 77 + voice 10 pytest 全绿；只配一个方言 instruct 后加载结果含完整默认矩阵

### 4.1 7 音色矩阵

| # | persona ID | 名称 | CosyVoice voice | 特质定位 | 语速 | edge-tts 降级 | 特殊能力 |
|---|-----------|------|----------------|---------|------|--------------|---------|
| 1 | xiaoxing | 小星 🌟 | longxing_v3 | 温婉邻家姐姐（女 20~25） | 1.0 | XiaoxiaoNeural | — |
| 2 | bobo | 波波老师 👩🏫 | longyingling_v3 | 温和共情女老师（女 25~30） | 0.95 | XiaohanNeural | — |
| 3 | yueliang | 月亮 🌙 | longwan_v3 | 细腻柔声讲故事（女 20~30） | 0.92 | XiaohanNeural | — |
| 4 | xiaotaiyang | 小太阳 ☀️ | longanyang | 阳光大哥哥（男 20~30） | 1.05 | YunxiNeural | **情感 Instruct** |
| 5 | dashu | 大树 🌳 | longanyun_v3 | 居家暖男/班主任（男 30~35） | 0.95 | YunyangNeural | — |
| 6 | doudou | 豆豆 ⚽ | longjielidou_v3 | 阳光顽皮同龄男孩（男童 10） | 1.05 | YunxiaNeural | — |
| 7 | qiqiu | 方言 🗣️ | longanhuan_v3 | 方言伙伴（女 20~30） | 1.05 | XiaoyiNeural | **方言 Instruct** |

**默认推荐**：男生 → xiaotaiyang；女生 → xiaoxing；未设置 → xiaoxing。
**UI 布局**：设置面板 grid-cols-4 两行（4+3），"方言"按钮琥珀色渐变特殊样式。

### 4.2 方言配置（v4 双模式：native / instruct）

| 方言 | mode | Instruct 指令 | 原生音色 | edge-tts 降级 |
|------|------|--------------|---------|--------------|
| 粤语 | native | — | longjiayi_v3（女）/longanyue_v3（男） | 无 → 普通话 |
| 闽南话 | native | — | longanmin_v3（男） | 无 → 普通话 |
| 东北话 | instruct | 请用东北话表达。 | — | zh-CN-liaoning-XiaobeiNeural |
| 四川话 | instruct | 请用四川话表达。 | — | 无 → 普通话 |
| 河南话 | instruct | 请用河南话表达。 | — | 无 → 普通话 |
| 山东话 | instruct | 请用山东话表达。 | — | 无 → 普通话 |
| 湖南话 | instruct | 请用湖南话表达。 | — | 无 → 普通话 |
| 陕西话 | instruct | 请用陕西话表达。 | — | zh-CN-shaanxi-XiaoniNeural |

**交互规则**：
1. 方言功能仅"方言"音色（qiqiu）选中时自动启用，无独立开关；其他音色时方言区域隐藏
2. `users.dialect`（管理端配置）仅决定默认选中项，不限制可选范围
3. 原生方言音色（粤语/东北话/陕西话）显示普通话/方言模式切换，默认普通话：普通话模式 = qiqiu + Instruct；方言音色模式 = 原生音色（无需 instruction）
4. 前端存储 localStorage：personaId + dialect + languageMode

### 4.3 情感 Instruct（仅小太阳 longanyang）

```
"你正在进行闲聊互动，你说话的情感是<情感值>。"
情感值：neutral、happy、sad、angry、fearful、surprised、disgusted
非标准情绪回退：calm→neutral、anxious→fearful、excited→happy
```

### 4.4 TTS 接口协议

`POST /api/v1/tts/synthesize`（WebSocket 流式返回，首包 ~0.8-1.5s；Java 后端 15s 超时兜底）：

```json
{
  "text": "你好呀",
  "persona": "qiqiu",
  "emotion": "happy",
  "speed": 1.0,
  "dialect": "cantonese",
  "language_mode": "dialect"
}
```

后端处理（**v4：language_mode 已废弃**，保留字段仅为向后兼容，不再生效）：`dialect` 命中原生方言音色（粤语/闽南）→ 自动替换 voice 无需指令；`dialect` 非空 + 音色 dialect_capable（qiqiu）→ Instruct；`persona=xiaotaiyang`（emotion_capable）无方言 → 情感 Instruct；方言与情感互斥，方言优先。

### 4.5 三级降级策略

```
Level 1: CosyVoice（DashScope SDK WebSocket 流式，模型 cosyvoice-v3-flash）
  ├─ Instruct（方言/情感）失败 → 无指令重试（保留音色品质）
  ├─ 不可用/超时（TTS_SYNTHESIZE_TIMEOUT=30s）/失败 →
Level 2: edge-tts 方言音色（仅东北/陕西有对应，其余方言降普通话）
    ↓ 失败或无对应（含 30s 超时防挂死）
Level 3: HTTP 503 → 前端自动降级浏览器 speechSynthesis（非静默）
```

> 详见 10 §6.8；方言能力：原生方言音色（粤语/闽南）自动生效，Instruct 方言需 qiqiu 音色。

---

## 5. Voice 服务配置（voice-service/config.yaml）

### 5.1 配置结构（ASR/SER 解耦，🟩 commit 2630be3 + 6630be3 + 44a923e 定稿）

```yaml
asr:
  # 引擎由环境变量 ASR_ENGINE 控制，默认 funasr（本地自部署，适合私有化/信创）
  funasr_model: "iic/SenseVoiceSmall"     # ASR_ENGINE=funasr 时使用（本地自部署）
  vad_model: "fsmn-vad"
  dashscope_model: "paraformer-realtime-v2"  # ASR_ENGINE=dashscope 时使用（云端，低资源占用）

ser:
  # 由 SER_ENABLED 控制，默认 true（独立于 ASR，解耦加载）
  model: "iic/emotion2vec_plus_large"     # ~1.5GB，CPU 推理

emotion_labels:   # 9 类情绪映射 [英文, 中文]
  - ["angry", "愤怒"]
  - ["disgusted", "厌恶"]
  - ["fearful", "恐惧"]
  - ["happy", "开心"]
  - ["neutral", "中性"]
  - ["other", "其他"]
  - ["sad", "悲伤"]
  - ["surprised", "惊讶"]
  - ["unknown", "未知"]
# voice-service 内存：由 docker-compose 的 VOICE_MEMORY_LIMIT 控制（默认 6G，docker-compose.prod.yml）
```

**架构解耦设计**（区别于上轮一体加载）：

- **ASR 与 SER 并行执行**（`ThreadPoolExecutor(max_workers=2)` + `executor.shutdown(wait=False, cancel_futures=True)`）：避免串行 3-4s 等待，典型响应<2s
- **ASR 引擎双可选**：funasr（自部署 SenseVoiceSmall，适合信创/私有化） / dashscope（云端 Paraformer-V2，低资源占用），仅二选一（互斥）
- **SER 独立加载**：无论 ASR 走哪条路径，SER 都走 funasr AutoModel 加载 emotion2vec_plus_large（dashscope 模式也会加载 funasr 包，仅用于 SER）
- **SER 启用可选**：`SER_ENABLED=false` 时完全不加载 emotion2vec，语音情绪默认中性
- **超时分层**：`VOICE_PROCESS_TIMEOUT=30s`（ffmpeg 转码）/ `VOICE_ANALYZE_TIMEOUT=60s`（ASR/SER 分析），避免 SDK/网络黑洞时请求永久挂起（P1-DEP 修复）
- **合规（22 §6.3 转写即删）**：`finally` 块中 `os.unlink()` 临时文件 + 记录删除数（避免文件占内存），原始音频不保留
- **P1-10 Prometheus metrics**：`/metrics` 端点手写文本格式（零新增依赖）：
  - `voice_analyze_requests_total{result="success|error|timeout"}` 请求总数
  - `voice_analyze_duration_seconds_sum/_count` 分析耗时 summary
  - `voice_asr_ready` / `voice_ser_ready` 引擎就绪 gauge

### 5.2 启动决策（env 先行）

| 环境变量 | 作用 | 为何必须 env |
|---------|------|-------------|
| `ASR_ENGINE` | 控制 ASR 引擎 | 决定启动时是否加载 SenseVoice（数 GB 内存决策） |
| `SER_ENABLED` | 控制情感分析 | 决定是否加载 emotion2vec（~1.5GB），且需在 torch 导入前确定 |
| `DASHSCOPE_API_KEY` | 云端 ASR/TTS 凭证（tts-service + voice-service） | 密钥 |
| `LLM_BACKUP_API_KEY` | LLM 降级供应商 key（OpenAI 兼容协议；2026-08-06 由 `LLM_FALLBACK_API_KEY` 改名，旧名回退） | 密钥（不再复用 DASHSCOPE_API_KEY，避免 key 泄露面过广 / 权限独立可吊销） |

### 5.2.1 模型部署与降级（2026-07-31，9340c6b）

- **宿主机缓存**：`prepare-funasr.sh` 预下载 FunASR 模型到宿主机 `MODEL_CACHE_DIR`（默认 `~/.cache/msasr`）——manifest.json 版本比对（python3 解析，无需 jq）+ 临时容器下载，容器重建不重复拉数 GB 模型；docker-compose 挂载 `MODEL_CACHE_DIR:/models`（只读）。部署脚本见 `deploy/scripts/prepare-funasr.sh`（EXPECTED_MODELS 全 pin master）。
- **fail-fast 不静默降级**：`entrypoint.sh` 在 ASR_ENGINE=funasr 时校验模型文件缺失即 **exit 1**（容器启动失败被 healthcheck/重启策略兜住）——**不自动切 dashscope**（避免密钥缺失时静默上云，隐私红线）；切换引擎必须显式改 `ASR_ENGINE` env 重建。
- **语音服务超时加固（d30bc00）**：ASR/SER 并行线程池（ThreadPoolExecutor(2)）+ 请求超时；转写完成即删临时音频（不落盘，COMP-009）。

---

## 6. 前端运行时配置注入 + 模型自托管（🟩 commit b717018 + d5c803 + b01305e + 461faf4）

### 6.1 运行时配置注入

- 新增 `frontend/student-h5/src/config/remote.ts`：`loadRemoteConfig()` 启动时 fetch `/api/v1/system/config`（3s 超时，失败静默降级），`getRemoteConfig()` 读取缓存
- 使用：`const threshold = getRemoteConfig().voiceprint?.verifyThreshold ?? VP_VERIFY_THRESHOLD`
- 本地 `config/*.ts` 保留：fallback 默认值 + PWA 离线场景 + 类型定义

### 6.2 ONNX 模型自托管（同源加载）

| 项目 | 详情 | commit |
|------|------|--------|
| 模型清单 | whisper-tiny（唤醒词识别，约 20MB）+ wespeaker（声纹识别，256-dim embedding） | b717018 + 5c6e399 |
| 投放位置 | `frontend/student-h5/public/models/whisper-tiny/` + `wespeaker/`（仅保留 json/量化 onnx/txt） | b717018 |
| 访问路径 | Vite 构建后随 dist 由 nginx 在 `/mindsafe/models/` 同源提供 | b717018 |
| 环境变量 | `WAKE_MODEL_REMOTE_HOST='SAME_ORIGIN'` / `VP_MODEL_REMOTE_HOST='SAME_ORIGIN'` | b717018 |
| 远程路径模板 | `env.remotePathTemplate='{model}/'`（修复默认带 `/resolve/main/` 导致 404） | d5c803 |
| 部署脚本 | `deploy/scripts/prepare-models.sh` 从 `hf-mirror.com` 下载 + 发布冒烟含脚本失败拦截 | e83ed9e（V24 阶段）+ 461faf4 |
| 部署保护 | `deploy.sh` rsync 排除 `models/` 目录，防止部署时误删自托管模型 | b01305e |
| 加载时序 | **预加载策略**（⚡ 提升体验）：①登录页预加载声纹模型 + 情绪选择页预加载唤醒模型（利用 TTS 播放时间窗口提前下载，commit d5d02bb + ef169e0 + 4f74ede）；②对话内首次触摸麦克风按钮后预热（替代挂载即预热，commit a6a2422） | d5d02bb + ef169e0 + a6a2422 |
| **WASM/SIMD/SAB 兼容性**（🟩 commit a81dbf4 + a184b4b + 5a95baf + 67fb0c8 + bb6e537 + aa1102c + 5d0deb3） | ① nginx 添加 COOP/COEP 头启用 SharedArrayBuffer；②禁用 asyncify 变体 + 入口 SW 清理；③`numThreads=1` ORT 单线程模式；④禁用 ORT 高级图优化（修复 `TransposeDQWeightsForMatMulNBits` 崩溃，commit 392a098）；⑤不支持 SIMD/SAB 时静默降级（不显示错误横幅+隐藏声音按钮）；⑥修正 SIMD 检测 WASM 二进制（原字节序列格式错误）；⑦不支持提示文字白色高对比度 | a81dbf4 + a184b4b + 5a95baf + 67fb0c8 + 392a098 |

### 6.3 加载失败与降级路径

| 场景 | 降级行为 |
|------|----------|
| 模型文件 404（未执行 prepare-models.sh） | 唤醒/声纹加载失败 → 隐藏唤醒开关 + 隐藏声音按钮；按住说话主路径不受影响 |
| 浏览器不支持 SIMD/SAB | 静默降级（无错误弹窗），UI 隐藏相关控件 |
| ASR/SER 服务不可达 | 后端返回 503 → 前端提示「语音服务不可用」 |
| TTS 服务不可用 | Level 1 CosyVoice 失败 → Level 2 edge-tts → Level 3 503 + 前端 speechSynthesis 兜底 |

---

## 7. 环境变量规范

### 7.1 引用格式

| 服务 | 格式 | 示例 |
|------|------|------|
| Java（Spring） | `${ENV_VAR:default}` | `${DB_PASSWORD:mindsafe_dev}` |
| Python（YAML） | `${ENV_VAR:default}` + 加载时解析 | `${DASHSCOPE_TTS_MODEL:cosyvoice-v3-flash}` |
| Docker Compose | `${ENV_VAR:-default}` | `${HTTP_PORT:-80}` |
| 前端 | 无环境变量（运行时 API 获取） | — |

### 7.2 归属决策矩阵

| 变量 | 决策 | 理由 |
|------|------|------|
| DB/REDIS 密码、JWT_SECRET、ENCRYPTION_*、各 API Key | ✅ 必须 env | 密钥凭证，12-Factor 铁律 |
| SMS_PROVIDER / MINDSAFE_CONSENT_TRIAL_AUTO_GRANT | ✅ 保留 env | 跨环境不同（dev=logging / prod=aliyun），免重建切换；AUD-010/DEP-001：监护同意开关必须带 MINDSAFE_ 前缀（旧名 CONSENT_TRIAL_AUTO_GRANT 已废弃，配置静默失效） |
| ASR_ENGINE / SER_ENABLED | ✅ 保留 env | 控制启动时模型加载 |
| VOICE/TTS_SERVICE_URL、HTTP/HTTPS_PORT、MODEL_CACHE_DIR、VOICE_MEMORY_LIMIT | ✅ 保留 env | 容器网络拓扑与 compose 直接使用 |
| CORS_ORIGINS | ✅ 保留 env | 环境差异大（localhost vs 生产域名） |
| GRAFANA_PASSWORD | ✅ 必须 env | 监控密码，缺省 monitoring compose fail-fast |
| LLM_PRIMARY_BASE_URL / LLM_PRIMARY_MODEL / DASHSCOPE_TTS_MODEL | 🟡 可选 env | 配置已有同默认值，仅切换供应商时覆盖（旧名 LLM_BASE_URL / LLM_MODEL 已 deprecated） |
| SMS_ALIYUN_SIGN_NAME / TEMPLATE_CODE、WECOM_CORP_ID/AGENT_ID/REDIRECT_URI | 🟡 可选 env | 半公开标识符，统一填写 |
| ALERT_WECOM_WEBHOOK_URL / MENTIONED | 🟡 可选 env | 配置端点，保留可接受 |
| WECOM_ALERT_TO_USER | 🟡 可选 env | 监控告警接收人；空 = 告警仅留存不推送 |

### 7.3 配置治理铁律

1. **docker-compose 是 env 到容器的唯一桥梁**：`.env` 定义但 compose 未透传的变量 = 死变量（历史缺陷 E1：WECOM 四变量未透传导致容器企微 OAuth 永远 501，已修复）
2. **启动决策不可延迟**：控制"是否加载数 GB 模型"的开关必须为 env
3. **env 可覆盖一切，但不应重复定义**：避免双源维护
4. **变量名必须与占位符完全一致**：`LLM_PRIMARY_API_KEY` 写成 `SPRING_AI_DEEPSEEK_*`、`TTS_SERVICE_URL` 写成 `MINDSAFE_TTS_SERVICE_URL` 时 Spring 松散绑定匹配不上，静默回落默认值
5. `.env.example` 所有变量带注释（用途 + 必填/可选），`.env` 不入 Git

---

## 8. 外部服务依赖总览

### 8.1 依赖清单

| 外部服务 | 用途 | 凭证 | 故障影响与降级 |
|---------|------|------|---------------|
| DeepSeek API | 主 LLM | `LLM_PRIMARY_API_KEY` | 自动降级备模型（ResilientChatModel 主备，`LLM_BACKUP_*`） |
| 阿里云百炼 | 备用 LLM | `LLM_BACKUP_API_KEY` | 主备双失败后安全话术 |
| DashScope（CosyVoice） | TTS 主力 | `DASHSCOPE_API_KEY` | 降级 edge-tts → 浏览器 speechSynthesis |
| DashScope（Paraformer-V2） | ASR 主力 | `DASHSCOPE_API_KEY` | 降级 FunASR 本地 |
| edge-tts（微软） | TTS 二级 | 无 | 降级浏览器合成 |
| 阿里云 SMS | 验证码短信 | `SMS_ALIYUN_ACCESS_KEY_ID/SECRET` | 当前 SMS_PROVIDER=logging（签名待申请） |
| 企业微信 | OAuth 登录 + 应用消息告警 | `WECOM_CORP_ID/AGENT_ID/SECRET` | 空配置 = 501/告警仅留存 |
| 阿里云 OSS | 文件存储（预留） | OSS 凭证 | — |

### 8.2 内部服务依赖（容器网络）

```
前端 → nginx → backend:8080（唯一公网入口，127.0.0.1 绑定防伪造 XFF）
backend → postgres:5432 / redis:6379
backend → tts-service:10096（TTS_SERVICE_URL）
backend → voice-service:10095（VOICE_SERVICE_URL）
tts-service → DashScope API / edge-tts（出网）
voice-service → DashScope API / 本地模型 volume（/home/appuser/.cache/modelscope）
```

### 8.3 风险与约束

| 风险 | 缓释 |
|------|------|
| 前端拉取配置失败 | 本地 fallback 默认值保底，PWA 离线不受影响 |
| 配置下发暴露内部参数 | 仅下发非敏感业务参数，密钥/内网地址绝不下发 |
| 引导脚本被篡改 | 后端 yml 由运维管控，前端只读消费 |
| Python YAML 解析依赖 | PyYAML 已在 requirements.txt（dashscope 间接依赖），零新增 |

---

## 9. 实施状态

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M1 | application.yml system-config 节点 + SystemConfigController + 前端 remote.ts + 声纹阈值去重 | 🟩 已完成（CFG-001~004） |
| M2 | tts-service/config.yaml（7 音色 + 8 方言 + 情感）+ Dockerfile COPY + compose 透传 DASHSCOPE_TTS_MODEL | 🟩 已完成（CFG-005~007） |
| M3 | voice-service/config.yaml + Dockerfile COPY | 🟩 已完成（CFG-008） |
| M4 | 文档同步（.env.example、DEPLOY-GUIDE、关联设计引用） | 🟩 已完成 |
| M5（fix-13） | tts-service requirements-lite 精简（P2-24：在线安装阿里云镜像加速，wheels 离线目录改为可选 refresh-wheels.sh 方案）+ 非 root 运行（P3-31）+ staging 死配置删除（P2-20）+ SMS_PROVIDER 默认统一（P0-5：dev=logging / 生产 compose 强制 aliyun；2026-08-06 修订：生产默认改 logging，待 BIZ-003 阿里云 SMS 签名/模板审核通过后再改 aliyun） | 🟩 已完成 2026-08-05（生产默认 logging 修订 2026-08-06） |
| M6 | LLM 主备配置通用化（doing/63：`LLM_PRIMARY_*`/`LLM_BACKUP_*` 任意双供应商 + `LlmExtraBodyConfig` 按 base-url 注入）+ 字段加密开关（doing/64：`ENCRYPTION_ENABLED` 默认 false，prod 无 key 不再崩溃） | 🟩 已完成 2026-08-06（counseling-ai 65 用例 + counseling-service 全量绿；生产验证主=deepseek-v4-pro 备=MiniMax-M3） |

前置检查全部通过：声纹阈值统一 0.70 单值（S4-1）✅；TTS v3-flash ✅；方言 v4 架构 ✅；ASR/SER 解耦 ✅；前端 578 测试 + Python TTS 35 测试全绿 ✅。

---

_合并自：design/57_配置统一纳管设计、design/56_TTS音色矩阵与方言设计 | 整合日期：2026-07-28_
