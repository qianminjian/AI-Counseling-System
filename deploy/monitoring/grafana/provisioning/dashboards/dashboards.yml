apiVersion: 1

providers:
  - name: 'MindSafe Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    # AUD-056：仪表盘只读（editable: false），防误改配置层数据
    editable: false
    options:
      path: /etc/grafana/provisioning/dashboards
